# M1a live acceptance — sanitized evidence bundle (M1a G6 PASS; G8 record, plan §13/§14)

Date: 2026-08-24. Source: the fifth owner-attended live cycle
`run-20260824-170359-58d9` on the owner's Ubuntu host, native subscription
authentication only. The raw run archive stays local-only under
`~/.agentteam/runs/` (owner-only modes, gitignored) and is never committed;
the bundle in [`m1a-live-2026-08-24/`](m1a-live-2026-08-24/) contains only
the output of the tested redaction function. This record sits deliberately
**outside** that directory so the bundle's `manifest.sha256.json` and its
fail-closed self-scan remain exactly what `sanitize_run_archive` produced.

## Regeneration

The bundle is byte-reproducible from the local archive:

```bash
uv run --no-sync python -c "
from pathlib import Path
from agentteam.run.sanitize import sanitize_run_archive
sanitize_run_archive(
    Path.home() / '.agentteam' / 'runs' / 'run-20260824-170359-58d9',
    Path('docs/evidence/m1a-live-2026-08-24'),
)"
```

`sanitize_run_archive` refuses a non-empty destination, redacts argv to typed
placeholders, records environment names only, and raises rather than write a
bundle that fails its own scan. The committed bundle was additionally
owner-reviewed and spot-checked (zero absolute paths, zero environment
values) before this commit.

## Amended gate and the Grok FAIL-HARD record (ADR 0036)

This acceptance ran under the owner-amended PoC A gate (ADR 0036): **the
live ensemble is the Claude Code and Codex legs plus a Claude Code
synthesis**. Grok's leg was amended out after four owner-attended cycles
recorded FAIL-HARD at task scale: headless `grok` 1.0.5 (model
`grok-4.6-build`) either answered in a single turn with an empty progress
snapshot or was vendor-`cancelled` at `num_turns: 2` with
`structuredOutput: null` — zero real reviews in four attempts, with
`--max-turns 40` falsified in-argv on the fourth (the cancellation is not
governed by the flag; the cause is unreachable from the recipe). Grok
remains fully present in profiles, probes, deterministic fakes, and the
compatibility surface; the all-three question returns on a future Grok CLI
release (RISKS R27/R33; the G5 probes that verified Grok's channels are
unchanged evidence).

## Acceptance summary (both tiers PASS)

| Tier | Conditions | Result |
| --- | --- | --- |
| Mechanical | cond-1 independent fresh legs, one bundle (`fd54eae7dbaa…`), targets unmutated; cond-6 package rehash; cond-7 manifest reconstructs; cond-8 names-only records, raw evidence local | **PASS** |
| Semantic | cond-2 each leg identified command injection plus other seeded defects with actionable rationales; cond-3 the union covers all three seeded defects; cond-4 no invented critical outside the oracle; cond-5 synthesis lists all defects with valid `invocation_id:finding_id` attribution (six merged findings, five agreements, one disagreement); cond-9 tiers stored separately | **PASS** |

All three invocations succeeded on attempt 1 with `schema_outcome: valid`
and empty problem lists (Claude leg 119.9s, Codex leg 69.7s, synthesis
40.1s); zero retries; exit 0.

## Call-spend ledger (hard ceiling 30, ADR 0020/0033)

| Spend | Calls |
| --- | --- |
| G5 attended probes (Claude 3 / Codex 2 / Grok 4) | 9 |
| G6 cycle 1 (2026-08-24, FAIL — schema dialect) | 3 |
| G6 cycle 2 (FAIL — synthesis attribution; mechanical tier first live PASS) | 4 |
| G6 cycle 3 (FAIL — Grok turn-cap) | 3 |
| G6 cycle 4 (FAIL — Grok turn-cap; `--max-turns` falsified) | 3 |
| G6 cycle 5 (**PASS**, this bundle) | 3 |
| **Total spent / remaining** | **25 / 5** |

Every cycle ran on a fresh, explicit owner confirmation at a green no-call
gate; no automatic rerun, retry beyond the single transient allowance, or
API-mode fallback occurred anywhere. Each remaining call is an individual
owner ceiling decision.

## Privacy boundary

Recorded argv uses typed placeholders; environment records carry names
only; raw streams, workspace copies, config homes, and absolute paths never
leave the local gitignored archive. The tested sanitizer produced every
file in the bundle, and its fail-closed scan plus the owner review gate ran
before this commit (plan §13; RISKS R22).
