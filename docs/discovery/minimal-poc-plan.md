---
title: Minimal PoC plan — PoC A/B/C definitions that prove or falsify the smallest layer
status: historical M0 PoC proposal with current constraints; detailed plan is provisional and not approved
date: 2026-08-22
owns: the historical M0 PoC A/B/C proposal plus confirmed constraints that any replacement plan must honor; no code and no approved implementation plan
depends_on: product-intent.md §4 (historical acceptance sketches plus current constraints); architecture-options.md §5 (the layer under test, restated verbatim in §1), §3, §6, §8; harness-broker-model.md §4, §7, §8; team-execution-model.md §2, §5, §6, §13; assistant-domain-model.md §3, §4, §8; existing-systems-fit-gap.md; reuse-vs-build-analysis.md; legacy-atm-disposition.md (K12, R4, R6, R11); evidence/*.md; evidence/panel/judge-{1,2}.md; .project-steward/QUESTIONS.md
---

# Minimal PoC plan

> **Provisional artifact.** Sections 2–8 preserve the detailed M0 PoC proposal for reference; they are not the approved next implementation plan. Run counts, harness allocation, synthesis, schemas, sequencing, gates, and LOC must be re-baselined. The confirmed constraints in §1.1 are authoritative over conflicting detail below.

## 1. Purpose and the layer under test

Discovery proposed the smallest layer in `architecture-options.md` §5. The current direction is restated below; the detailed M0 test design that follows remains a candidate, not an implementation authorization.

**The smallest new software layer is** a thin, format-independent Assistant/Team layer — working name `ats` — that owns only the data no existing system has and reaches everything else through two declared seams. It owns, as substrate-neutral data: Assistant definitions (closed schema + exclusion validator), TeamTemplates composing Assistants by reference, Base/User/Reviewed-Evolution overlays with a deterministic resolver and a Proposal/review record, HarnessProfiles plus a HarnessSelectionPolicy resolved per invocation (user > Assistant > team > default), a HarnessInvocation ledger and an Ensemble record with per-invocation attribution, an artifact manifest + lock + per-host resolution report, and a TeamRun record with user-visible and archive rosters, a dynamic-member gate, a nesting contract, and an archive. It declares **HarnessAdapter** {profile data, injection recipe, invoke, parse} and **CoordinationSubstrate** {create_space, add_member, create/update/wait task, send/receive, snapshot, stop, cleanup}. Five harness targets remain planned; Claude Code, Codex, and Grok Build are required in the first pass, while Hermes and OpenClaw are deferred. The single planned CoordinationSubstrate implementation is a ClawTeam CLI adapter (pinned 0.3.0@0119833, `mcp<2`, subprocess backend, no tmux, never `launch`, never `--task` through `spawn`, `skip_permissions=false`), plus a small `direct` launcher for solo runs and independent harness invocations. The HarnessBroker lives inside this layer, not as a ClawTeam backend or plugin. The layer builds no second team substrate, ClawTeam fork, library-seam coupling, Surface adapter, artifact installer, UI, message bus, task store, or liveness registry. ClawTeam supplies the task DAG, inboxes, pid registry, worktrees, and snapshot; harnesses supply execution. **M (≈4–6k LOC including profile data) remains a discovery estimate; the first-pass slice, detailed PoC runs, synthesis design, implementation language, sequencing, and slice LOC must be re-baselined before implementation.**

### 1.1 Confirmed constraints for the next PoC plan

- First-pass harnesses: Claude Code 2.1.241, Codex 0.149.0, and Grok Build 1.0.5. Hermes/OpenClaw are deferred.
- Native/unattended live runs: subscription OAuth on the owner's persistent host. Claude uses `--safe-mode --no-session-persistence`; `--bare` disables OAuth. Hosted CI receives no live credentials.
- API-test mode: separate, replaceable provider profile; current `stealth/ox-alpha` is temporary and unverified. Store only the credential environment-variable name, never a key value. Do not fall back from native auth to this mode.
- Cross-platform: GitHub-hosted Windows/macOS CI verifies deterministic direct-path plumbing at its milestone and ClawTeam subprocess plumbing at its milestone. It cannot prove live auth/model behavior.
- Enforcement: bypass-visible/audited advisory controls are acceptable for PoC evidence; production claims require mechanical enforcement. UI hiding is a projection.
- ATM: internal copy/adaptation is authorized; preserve provenance and third-party notices.

[ev:m0-product-architecture-review-2026-08-22#F1][ev:m0-product-architecture-review-2026-08-22#F2][ev:m0-product-architecture-review-2026-08-22#F3][ev:m0-product-architecture-review-2026-08-22#F4][ev:m0-product-architecture-review-2026-08-22#F5][ev:m0-product-architecture-review-2026-08-22#F7]

| Component | Responsibility | Size | What it reuses (substrate primitive) | Evidence |
|---|---|---|---|---|
| Assistant definition package | closed schema (unknown keys rejected), exclusion/content validator, `id@version` + content hash | S ~0.4k | **new** data; shape ideas DG `.strict()`, Hermes `distribution.yaml` | [reuse:G-AD-01][reuse:G-AD-05][ev:dsh-agent-teams-and-gui#F20][ev:claude-agent-teams-hermes-openbot#F24] |
| TeamTemplate by reference | members/Lead/relationships, independence declaration, visibility defaults, dynamic-member policy, team preferences, workflow skeleton | S ~0.3k | **new** data; rendered into ClawTeam calls at run time | [reuse:G-TC-01][reuse:G-TC-05][model:team-execution-model#2] |
| Overlay resolver + Proposal/review record | Base → Reviewed Evolution → User merge with per-field policy + hash; Proposal with provenance, independence check, `applied_hash` | S ~0.4k (resolver in PoC-A slice; generator/review CLI deferred) | **new**; Hermes two-layer ownership split as precedent; ClawTeam plan round-trip optional transport | [reuse:G-EV-02][reuse:G-EV-03][model:assistant-domain-model#9][ev:clawteam-model#F17] |
| HarnessProfiles + HarnessSelectionPolicy resolver | capability rows with verification levels; resolve user > Assistant > team > default; fallback = new invocation | S ~0.4k + data | **new** data model; seeded from the evidence checklists; CTF `model_resolution.py` precedence pattern (rung 4) | [reuse:G-HB-01][reuse:G-HB-03][ev:harness-cli-capabilities-a#F16][ev:harness-cli-capabilities-b#F23][ev:clawteam-openclaw-fork-delta#F15] |
| HarnessAdapter seam + 5 planned adapters | per harness: profile data, injection recipe (flags vs prepared dir, `degraded[]`), invoke, parse session id/usage/outcome | M discovery estimate; Claude Code + Codex + Grok Build required first pass; Hermes/OpenClaw deferred | wraps each harness's own flags; an explicit positional command suppresses ClawTeam's implicit profile [ev:clawteam-probe-log#F17] | [model:harness-broker-model#10][ev:m0-product-architecture-review-2026-08-22#F3][ev:harness-cli-capabilities-b#F24] |
| HarnessBroker + `direct` launcher/wrapper + ledger + Ensemble record | resolve → bundle + `bundle_hash` → invoke → tee stdout/stderr + exit code → record; fan-out + synthesis invocation | S–M ~0.6k | **new** loop and record; wrapper shape from CTF `subprocess_wrapper.py`; fan-out via `direct` (ClawTeam optional) | [reuse:G-HB-05][reuse:G-HB-07][ev:clawteam-spawn-platform#F21][ev:clawteam-probe-log#F14][ev:harness-cli-capabilities-a#F10] |
| Artifact manifest + lock + resolution report | capability vs artifact vocabulary; kinds; vendored/git sources; lock `{ref, resolved, digest, fingerprint}`; per-host native/installed/degraded/unsupported | S ~0.3k | **new** data; ATM ADR 0022 ideas; OpenClaw eligibility, Hermes `env_requires` as inputs | [reuse:G-AR-01][reuse:G-AR-06][ev:atm-salvage#F10][ev:harness-cli-capabilities-b#F11] |
| CoordinationSubstrate seam + ClawTeam-CLI adapter | create_space/add_member/task/wait/send/receive/snapshot/stop/cleanup over `clawteam team spawn-team`, `task create/update/wait`, `inbox send/receive`, `spawn subprocess -- ats-wrap …`, `team snapshot/cleanup`; state read via `--json`/snapshot; only `spawn_registry.json` read directly | S ~0.6k | wraps ClawTeam CLI (rung 2); DAG, inboxes, registry, worktrees, snapshot are ClawTeam's | [ev:clawteam-probe-log#F7][ev:clawteam-probe-log#F9][ev:clawteam-probe-log#F12][ev:clawteam-probe-log#F16][ev:clawteam-probe-log#F22][ev:clawteam-model#F22] |
| Run layer | `run.json`; roster projections (visible vs archive); dynamic-member gate + Ephemeral Assistant writer; nesting contract (parent link, delegated task, RunResult recorded in the outer `run.json` + inner archive, signalled by `inbox send`, delegated task closed by `task update --status`, `independence.achieved`, stop-before-cleanup); archive writer | M ~1.0k (gated on PoC A) | **new** objects over ClawTeam mechanics (no `task update --metadata` — not a CLI verb) | [model:team-execution-model#5][model:team-execution-model#6][reuse:G-TE-05][ev:clawteam-probe-log#F18][ev:clawteam-probe-log#F19][ev:clawteam-probe-log#F20] |
| CLI + Lead/Member Skill + `CLAWTEAM_BIN` shim | `ats assistant/template/run/member/nest/result/archive`; SKILL.md telling a local Claude Code/Codex conversation when to call `ats`; coordination section rendered per substrate into the bundle; run-scoped shim refusing raw `spawn` | S ~0.3k + docs | wraps ClawTeam's own Skill (rung 1) | [ev:clawteam-model#F16][fitgap:MS-01][ev:clawteam-spawn-platform#F16] |
| Vendored modules | `platform_compat.py` (pid_alive, locks, Windows defaults), `model_resolution.py` pattern, `spawn_with_retry`; attribute HKUDS | S ~0.2k copied | rung 4, MIT | [ev:clawteam-openclaw-fork-delta#F19][ev:clawteam-openclaw-fork-delta#F28][ev:clawteam-openclaw-fork-delta#F30] |

The architectural safety constraints remain: never ClawTeam `launch`, never `--task` through `spawn`, and `skip_permissions=false`. The historical table is retained as an estimate; its slice/order claims are provisional and its earlier byte-identical relationship to `architecture-options.md` is no longer a current validation invariant.

**What the PoCs exercise and do not exercise, by layer (stated, not hidden).** AD, TC, TE, HB: the criteria of §3–§5 (the register IDs named in each "Proves / falsifies"). EV: only the EV-04 leak check (PoC A/B `defhash` and PoC C criterion 4); the overlay resolver is built in the slice and only informationally checked (§3); Proposal generation and review (EV-01..03, EV-05) come after PoC C and are falsified by no PoC. AR: one vendored `code-review` SKILL.md per reviewing Member (AR-01 partially, AR-04 by env-name credentials); manifest, lock and resolution report (AR-02, AR-03, AR-06) are built, not checked by any criterion. LO: operational mode (LO-01..04) is deferred after PoC C [model:architecture-options#5] — brief items 8–10 are therefore partial here by design. MS: no Surface is exercised (product-intent §4).

## 2. Historical M0 common setup (provisional)

**Current baseline.** Ubuntu host with no tmux; Claude Code 2.1.241, Codex 0.149.0, and Grok Build 1.0.5 on PATH; ClawTeam remains 0.3.0@0119833 with `mcp<2` for team-path work. Native live runs use the owner's existing subscription OAuth and never place credentials in hosted CI. An API-test provider is optional and separate; no API key is a prerequisite for documentation or deterministic tests [ev:m0-product-architecture-review-2026-08-22#F1][ev:m0-product-architecture-review-2026-08-22#F2].

**Created throwaway under one root `$POC`.** `definitions/` (the Assistants and TeamTemplate under test, `git init`-ed so `git status --porcelain` is a second unchanged check); `fixture*/` target repos; `runs/<run-id>/` (`run.json`, per-Member workspaces, rendered bundles, teed stdout/stderr); `ledger/` (HarnessInvocation + Ensemble records); per-harness throwaway state `CLAUDE_CONFIG_DIR=$POC/state/claude`, `CODEX_HOME=$POC/state/codex`, a cloned Hermes profile under `$POC/state/hermes` [ev:harness-cli-capabilities-a#F19][ev:harness-cli-capabilities-b#F19]; for ClawTeam `HOME=$POC/state/ct-home` (its `config.json` is always `~/.clawteam/config.json` [ev:clawteam-model#F2]) with `default_backend: subprocess`, `skip_permissions: false`, and `CLAWTEAM_DATA_DIR` per run or shared (§5) [ev:clawteam-probe-log#F3].

**Never touched.** `~/.clawteam`, `~/.claude`, `~/.codex`, `~/.hermes`, `~/.openclaw`, `~/.grok`; the reference checkouts under `/home/wsh/Documents/00000/`; this repository's `docs/`. A fail-closed guard runs before the first subprocess of each PoC: every state variable must resolve under `$POC`, else abort — ATM's disposable-profile practice, kept as practice not code [model:legacy-atm-disposition#2][ev:atm-salvage#F20]; after each PoC a newer-than-start scan of the production dirs must be empty (R6 [model:legacy-atm-disposition#4]).

**"Definition unchanged" assertion.** `defhash` = SHA-256 over sorted relative paths and bytes of `$POC/definitions/` (modes excluded), snapshotted before the first invocation, after every run and after cleanup; PASS requires all snapshots equal and `git status --porcelain` empty. Bundles are therefore copies — never symlinks into `definitions/` — the AT-13 precedent [ev:atm-salvage#F19][model:assistant-domain-model#4].

**How HarnessInvocations are recorded.** One JSON record per invocation in the shape of [model:harness-broker-model#7]: `invocation_id, run_id, member, ensemble_id, assistant_ref{id, version, effective_hash}, backend{name, version from --version}, selection{decided_by, candidates}, injection{bundle_id, bundle_hash, render{part→channel}, degraded[]}, command{argv_redacted, cwd, env_names, launcher}, harness_session{id}, timing, usage{tokens, cost_usd, cost_source}, outcome{status, exit_code}, artifacts[], output_ref`. Opened with `status: started` before the process starts and closed on exit, so an aborted invocation still leaves a terminal record (ATM R4 [model:legacy-atm-disposition#4]). The wrapper (`direct`, or `ats-wrap` under ClawTeam) tees stdout/stderr and captures the exit code, because ClawTeam discards both and journals `exit_code: null` [ev:clawteam-spawn-platform#F21][ev:clawteam-probe-log#F14][reuse:G-TE-07]. Fields per harness: Claude `--output-format json` → `session_id`, `total_cost_usd`, `modelUsage`; Codex `--json` → `thread.started`, token counts, no USD (`cost_source: unavailable|derived`, Q9) [ev:harness-cli-capabilities-a#F10]; Hermes `-z --usage-file` [ev:harness-cli-capabilities-b#F7].

**Two tiers.** Tier 0: a deterministic no-op "harness" script that accepts the rendered argv, echoes `bundle_hash`, writes schema-valid canned output and exits as instructed — plumbing, records and hashes at zero cost (ATM R11; [panel:judge-2.md#7]). Tier 1: the real harnesses. Only Tier 1 can PASS (product-intent §4 demands real reviews and a completed task); Tier 0 is a precondition unless Q8 says otherwise.

**Interface probes required by any replacement plan.** Codex instruction injection on 0.149.0; Claude headless execution with `--safe-mode --no-session-persistence`; Grok definition/system-prompt and structured-output injection; Hermes hiding only if Hermes enters scope. GitHub-hosted Windows/macOS jobs test deterministic OS plumbing without live credentials [ev:m0-product-architecture-review-2026-08-22#F3][ev:m0-product-architecture-review-2026-08-22#F5].

## 3. Historical M0 PoC A proposal — reusable Assistant, interchangeable harness

> This two-leg/run-three design is not the current first-pass plan because it omits Grok Build. Preserve it as a candidate only; do not infer final run count, synthesis harness, schemas, or sequence from this section.

**Substrate.** The `direct` launcher; no ClawTeam team, no DAG. **Definition under test.** `code-reviewer` per [model:assistant-domain-model#3]: `persona.md`, `principles.md`, `skills: [code-review]` (one vendored SKILL.md), `permissions {filesystem: read-only, network: deny, shell: allow}` (one intent: any shell command, no filesystem writes — narrowed from the `read-write-workspace` of [model:assistant-domain-model#3] for a review-only run), `harness_policy {preferred: [codex, claude-code], requires_capabilities: [headless, structured_output]}`, `preferences.output: findings by severity`. No User Overlay in any pass criterion; user-level overrides arrive only as run flags (below). The overlay resolver is therefore built but not exercised by PoC A's criteria; one informational Tier-0 check after Run 2 — a one-line User Overlay on `preferences.output` in a throwaway overlay directory → `assistant_ref.effective_hash` differs from the base hash while `defhash` is unchanged — shows that it runs; it is not a pass criterion.

**Inputs (fixed).** `$POC/fixture-a/`: a throwaway git repo (small Python package with tests) whose last commit plants three defects (an off-by-one, an unsafe shell call, a behaviour change without a test); target = `target.diff` (`git diff base..head`) plus the checkout at `head`; `target_sha256` recorded. Each invocation gets its own workspace copy. Identical positional task text for every run: "Review `target.diff` in this workspace; answer per the schema." Review schema: `{target_sha256, findings[]{id, severity, file, line?, title, rationale}, summary, verdict}`. Synthesis schema: `{inputs[], agreements[]{title, sources[]}, disagreements[]{title, asserted_by[], not_asserted_by[], severity_conflict?}, merged_findings[]}` with `sources`/`asserted_by` holding invocation ids.

**Injection per harness** (definition-injection matrix [model:harness-broker-model#4][ev:harness-cli-capabilities-a#F17]). The bundle — system text (persona + principles + collaboration), skills dir, permission intent, output schema — is rendered once and hashed (`bundle_hash`), then rendered into channels:

| Part | Run 1 — Codex 0.149.0 | Run 2 — Claude Code 2.1.241 |
|---|---|---|
| persona, principles, collaboration | `-c developer_instructions=<text>` (observed; probe (a)); fallback `$CODEX_HOME/AGENTS.md` → `degraded: [system-prompt-as-file]` | `--append-system-prompt-file <bundle/system.md>` (probe (b)); fallback `--append-system-prompt <text>` |
| skills | copied to `<ws>/.agents/skills/code-review/` | `--add-dir <bundle>` containing `.claude/skills/code-review/` |
| permissions (one intent for both cells: shell allowed, filesystem read-only) | `-s read-only -a never` — native rendering of the intent | `--permission-mode dontAsk --allowedTools Read,Grep,Glob,Bash(git diff*),Bash(git log*)` (illustrative allow-list) — the evidence lists no Claude flag for a read-only filesystem independent of tool scope [ev:harness-cli-capabilities-a#F17], so shell is narrowed to read-only commands and `degraded: [shell-narrowed]` is recorded; exact allow-list UNVERIFIED until probe (b) |
| isolation (TE-02) | `--ephemeral --ignore-user-config`, `CODEX_HOME=$POC/state/codex` | `--safe-mode --no-session-persistence`, isolated `CLAUDE_CONFIG_DIR` as needed |
| output | `--json --output-schema review.schema.json -o <ws>/review.json -C <ws>` | `--output-format json --json-schema review.schema.json` |
| record | thread id, tokens, `cost_source: unavailable` (or `derived`, Q9), `codex --version` | `session_id`, `total_cost_usd`, `modelUsage` |

**Historical bundle-equivalence rule.** Two invocations carry "the same definition" iff they share the pre-render `bundle_hash`, with render channels/degradation recorded. Any replacement plan must apply subscription-compatible isolation: Claude `--safe-mode --no-session-persistence`, Codex `--ephemeral --ignore-user-config`, and an isolated harness home/profile where applicable [ev:m0-product-architecture-review-2026-08-22#F3].

**Run shapes.** *Run 1* `ats run --assistant code-reviewer --target $POC/fixture-a` with no user override → the broker resolves the Assistant-level policy → codex, `decided_by: assistant`. *Run 2* with user-level `--harness claude-code` → `decided_by: user` (HB-03 precedence exercised). *Run 3* user-level `--ensemble claude-code,codex`: two legs sharing one `bundle_id`, each in its own workspace and state dir, run independently (no shared files, no message edge); then a third recorded invocation — the synthesis — whose prompt carries both `review.json` labelled by invocation id and whose schema is the synthesis schema; legs carry `ensemble_id`, the synthesis carries `inputs: [leg ids]` [model:harness-broker-model#8]. `synthesis_harness` defaults to claude-code (structured output + reported cost) until Q9 is answered; the choice and its bias are recorded in the Ensemble record.

**Success criteria** (product-intent §4 verbatim → measurable check).
1. "the definition file is byte-identical before and after all three runs" → `defhash` equal at all five snapshots; `git status --porcelain` empty.
2. "each run produces a review of the same target" → `review.json` validates for Run 1, Run 2 and both Run 3 legs; each `target_sha256` equals the fixture's.
3. "Run 3's synthesis lists agreements and disagreements with per-harness attribution" → synthesis output validates; every `sources`/`asserted_by` id resolves to a ledger record naming harness + version; `agreements[]` non-empty and `disagreements[]` non-empty or an explicit recorded "none".
4. "each HarnessInvocation is recorded" → exactly five closed records (Run 1, Run 2, leg a, leg b, synthesis), each with terminal status, non-null exit code, `decided_by`, teed output, and — decisive — the same `bundle_hash` on Run 1, Run 2 and both legs: the proof that different channels carried one definition [model:harness-broker-model#4].
Informational: planted-defect recall; Codex `degraded[]`.

**Proves / falsifies.** PASS proves AD-04, AD-08, HB-02 (two harnesses), HB-03, HB-05, HB-07 on the PoC-A slice. FALSIFIED if no channel reaches Codex without editing the definition (both `-c` and `AGENTS.md` fail — HB-02 [fitgap:HB-02]); if any harness writes into `definitions/` (AD-05 enforcement); if the synthesis cannot attribute (HB-05/HB-07).

**Cost/time bound.** Tier 1 = five invocations over a ≤300-line diff; `--max-budget-usd 2` per Claude invocation [ev:harness-cli-capabilities-a#F10]; ≤ USD 10 per cycle (estimate), ≤ 3 cycles, ≤ 45 min wall per cycle. Tier 0 is free.

## 4. Historical M0 PoC B proposal — reusable Team + dynamic hidden Member

**TeamTemplate** `development` {lead: implementation-planner, implementer, reviewer: code-reviewer} per [model:team-execution-model#2], with `dynamic_members {may_create: [lead], max_count: 2, allowed_harnesses: [codex, claude-code]}` (+ hermes iff Q6), `visibility_defaults.dynamic_members: hidden`, `independence: reviewer↔implementer level: advisory` (mechanical is not claimed), `workflow_skeleton` t1 Plan (lead) → t2 Implement (implementer, blocked_by t1) → t3 Review (reviewer, blocked_by t2) → t4 Report (lead, blocked_by t3). Supplied at instantiate: throwaway repo `$POC/fixture-b/` and task "add a `--json` output flag to the CLI, with tests". Harness binding: lead → Claude Code, implementer → Codex, reviewer → Codex, or Hermes `chat --source tool -q … -p <cloned profile>` per Q6 [ev:harness-cli-capabilities-b#F6] — ≥2 harnesses either way. The Lead creates one hidden temporary specialist `testgap` (Ephemeral Assistant: "find and add the tests the implementer missed") on Codex, task t2b `blocked_by t2`.

**Substrate mechanics** (ClawTeam CLI, subprocess backend, no tmux; never `launch`, never `--task`; [model:architecture-options#3] PoC B). Env `CLAWTEAM_DEFAULT_BACKEND=subprocess`, `CLAWTEAM_DATA_DIR=$POC/runs/B/ct`, HOME as §2. (1) `ats run development --repo $POC/fixture-b --task …` writes `run.json` before any process [model:team-execution-model#3]. (2) The *layer* — no Lead process exists yet — mints the Lead's ClawTeam identity (`CLAWTEAM_AGENT_ID`, `CLAWTEAM_AGENT_NAME=lead`) in its own env and runs `clawteam team spawn-team run-B --agent-name lead`, so the Lead Member is ClawTeam's leader; leader-ness is thereafter decided by *name* (`is_leader = _name == leader_name`) [ev:clawteam-model#F5], so the `spawn … -n lead` of step (4) runs as leader even if it mints a second `agentId` — the layer records every ClawTeam id it sees for `lead` in the roster entry (open question 1, settled in Tier 0); if `spawn -n lead` into that team is refused, fallback: the first `spawn` auto-creates the team with the Lead as ClawTeam's leader [ev:clawteam-probe-log#F18]. (3) `clawteam task create run-B … --blocked-by <real id>` for t1..t4, ids validated by the layer since ClawTeam accepts dangling ones [ev:clawteam-probe-log#F7]. (4) Per Member: `clawteam spawn subprocess --team run-B -n <m> [--repo $POC/fixture-b | --no-workspace] --no-keepalive -- ats-wrap <inv-id>` — an explicit positional command, so no implicit profile [ev:clawteam-spawn-platform#F5] and no `-p` append: ClawTeam builds a prompt only `if task:` and the generic adapter branch appends `-p` only `elif prompt:` (`ClawTeam/clawteam/cli/commands.py:3251-3255`, `ClawTeam/clawteam/spawn/adapters.py:131-140`; [panel:judge-2.md#5] item 2; the subprocess backend runs any executable [ev:clawteam-probe-log#F12]); `ats-wrap` reads the bundle from a file (persona + principles + a coordination section rendered with the exact `clawteam task/inbox` verbs and run id, Q10), launches the harness headless with the isolation and output rows of §3, stdin from `/dev/null`, and tees output. *Spawn timing:* the Lead is spawned at run start; implementer, reviewer and `testgap` are spawned **on unblock** — the run layer's observer issues the `spawn` when `clawteam task list run-B --json` shows the Member's task unblocked — because a one-shot `codex exec` spawned at start would return before its task unblocks, firing an `on-exit` reset and paying for nothing [ev:clawteam-probe-log#F14]; spawn-all is a Tier-0 variant only. *Permissions are rendered per Member from that Member's own Assistant intent*, not copied from §3's reviewer row: the Lead gets shell access to `ats` and `clawteam` (Claude `--allowedTools … Bash(ats *),Bash(clawteam *)`, illustrative), implementer and `testgap` get Codex `-s workspace-write -a never`, the reviewer keeps §3's read-only rendering. Every `spawn` exit fires `lifecycle on-exit` (task reset + "exited unexpectedly" to the ClawTeam leader even on exit 0) — accepted noise, described in the Lead's bundle [ev:clawteam-probe-log#F14]. (5) The Lead runs as one long headless invocation (`--max-turns`, budget cap) and coordinates through `clawteam task update/list`, `inbox send/receive`; mid-run it calls `ats member add --hidden --from-ephemeral $RUN/members/testgap.md --harness codex`; the gate evaluates `dynamic_members` **before** the substrate `spawn`, writes the Ephemeral Assistant into the run directory and records `visibility: hidden, origin: ephemeral, created_by: lead, policy_decision` [model:team-execution-model#5][model:assistant-domain-model#8]; a run-scoped PATH-shadowing `clawteam` shim (also set as `CLAWTEAM_BIN`, which only ClawTeam's own rendered prompt text honours [ev:clawteam-probe-log#F12]) refuses raw `clawteam spawn` from Members — TC-05 = gate + convention + run-scoped `CLAWTEAM_BIN`/PATH shim + post-hoc roster/registry reconciliation; the Lead *can* bypass it, since no pre-spawn hook exists on the CLI path [ev:clawteam-spawn-platform#F16][panel:judge-2.md#5]. (6) Completion via `clawteam task wait run-B --agent ats-observer` (explicit `--agent` avoids draining the leader inbox [ev:clawteam-model#F8]) plus registry liveness [ev:clawteam-probe-log#F13]. (7) Archive: `team snapshot run-B`, SIGTERM surviving pids from `spawn_registry.json`, `team cleanup` [ev:clawteam-probe-log#F20].

**Success criteria** (§4 verbatim → check).
1. "the TeamTemplate is unchanged" → `defhash` (template + the three Assistants) equal before/after; git clean.
2. "the run archive shows the hidden member (auditable) while the user-facing member list does not" → archive roster has `testgap {visibility: hidden, origin: ephemeral, created_by, policy_decision, definition_ref}` with its invocation record and teed output, and the reconstruction test (rebuild argv + bundle from the archive alone) succeeds; `ats status` / the visible projection lists exactly lead, implementer, reviewer. Honesty label: ClawTeam's `team status`/`board show` still lists `testgap` — two rosters, accepted for the PoC [ev:clawteam-probe-log#F10][fitgap:TC-04][panel:judge-2.md#5].
3. "the task completes through the DAG" → t1..t4 and t2b `completed`; task history/events show each dependent unblocked only after its blocker (auto-unblock [ev:clawteam-probe-log#F7]); the fixture contains the flag and its tests.
4. "at least two members use different harnesses" → ledger `backend.name` differs between lead and implementer; `spawn_registry.json` entries resolve through `ats-wrap` to distinct harness binaries [ev:clawteam-probe-log#F16].
Plus: every Member has a closed record with non-null exit code; no `launch`/`--task` in any recorded argv; `spawn_registry.json` reconciles against the roster with no unregistered spawn; `independence {declared: advisory, achieved: namespace}` recorded (one shared `CLAWTEAM_DATA_DIR`; vocabulary as in §5).

**Proves / falsifies.** TC-01, TC-05 (at the labelled level), TE-01, TE-03, TE-04, TE-07, AD-07; TE-08 for Ubuntu-without-tmux. FALSIFIED if a Codex `exec` Member cannot run under ClawTeam `spawn subprocess -- ats-wrap` (the precondition is probe (a) of §2 — `codex exec` + positional prompt without a TTY under the wrapper [ev:harness-cli-capabilities-a#F1]; ClawTeam's own bare `codex <prompt>` path, open question 1 of [ev:clawteam-spawn-platform], is not taken); if `on-exit` resets corrupt the DAG beyond what the layer can absorb; if the hidden Member cannot be kept out of the layer's view while fully present in the archive.

**Cost/time bound.** Tier 1: five or six invocations, the Lead long-running; ≤ USD 25 per cycle, ≤ 60 min, ≤ 3 cycles.

## 5. Historical M0 PoC C proposal — nested TeamRun

**Shape.** Outer TeamRun `ops-lite` on the ClawTeam substrate with one visible Member `codemod` (Assistant `code-modification`, Claude Code) and outer tasks t4 "eliminate the dataloader OOM in `fixture-c`" and t5 "report" (blocked_by t4). CodeMod judges t4 complex and runs `ats nest development --for-task t4`; the inner TeamRun is the PoC B template on mixed harnesses; the result returns; the inner run is archived; the outer run continues to t5.

**Historical mechanics.** The inner run records parent/depth/isolation, uses its own ClawTeam namespace, returns a RunResult through the outer `run.json` plus `inbox send`, closes the delegated task, snapshots, stops processes before cleanup, and links the inner archive. Fresh contexts use Claude `--safe-mode --no-session-persistence`, Codex `--ephemeral`, or isolated profile/home controls. Advisory `namespace`/`data-dir` isolation is acceptable only when its bypassability is recorded; production requires mechanical enforcement.

**Success criteria** (§4 verbatim → check).
1. "the inner run's tasks/messages are isolated from the outer run" → no inner DAG task and no inner *content* message appears in the outer team's tasks/inboxes/events; ClawTeam lifecycle notices (on-exit/idle/join/shutdown message types) addressed to the inner team's leader are excluded by type, and the check is evaluated on the archive's filtered projection recorded by the layer (the raw `events/` and inbox files are archived beside the projection; the on-exit notice carries ClawTeam's fixed text "Agent '<name>' exited unexpectedly …" [ev:clawteam-probe-log#F14] and is tagged by that text); inner tasks exist only under `run-C1`; outer task list = {t4, t5}; `independence.achieved` recorded honestly (`namespace` or `data-dir`, never `mechanical`).
2. "the result is returned to the creating Member" → RunResult JSON present in the outer `run.json` (`nested_runs[].result`) and in the inner archive; the RunResult summary message received in `ops-lite`'s `inboxes/codemod`; outer t4 `completed` through `task update --status completed` (with the optional `--description` pointer); CodeMod's teed output shows it consumed the result; the outcome of each carrier leg (record, signal, close) recorded.
3. "the inner run is archived" → inner `run.json` terminal; snapshot file present after `team cleanup`; `teams/run-C1` gone; no live inner pid; outer archive links it.
4. "nothing of the inner run leaks into any persistent definition" → `defhash` unchanged; inner Ephemeral Assistants only in the inner archive; grep for run-specific facts over definitions and throwaway harness state → 0 hits; subscription-compatible isolation applied [ev:m0-product-architecture-review-2026-08-22#F3].

**Proves / falsifies.** TE-05, TE-07, EV-04. FALSIFIED if the `inbox send` signal is not consumed by the creating Member or `task update --status completed` cannot close the delegated task with real Members (carrier change, layer-internal — the `run.json` record needs no substrate); if variant (ii) breaks waiting *and* the owner requires more than `namespace` (Q3 → ACL PR or spawn-path ownership, [model:architecture-options#6]); if inner facts appear in definitions or state (EV-04).

**Cost/time bound.** PoC B plus two short outer invocations: ≤ USD 35 per cycle, ≤ 90 min, ≤ 3 cycles.

## 6. Historical platform matrix and current CI disposition

| | Ubuntu 20.04, no tmux (this host) | macOS | Windows native |
|---|---|---|---|
| Direct path (Claude Code + Codex + Grok Build required first pass) | interface flags verified locally; no live model claim from this documentation pass | GitHub-hosted CI at the direct-path milestone: deterministic paths, quoting, exit handling, records/archives; no live credentials | same hosted-CI scope; no live auth/model claim |
| ClawTeam CLI subprocess path | core subprocess/task/inbox/registry mechanics probe-verified on Ubuntu | GitHub-hosted CI at the ClawTeam milestone; deterministic/no credentials | same; `skip_permissions=false`, no `--task` through spawn; deterministic/no credentials |
| Hermes Member (Q6) | `chat --source tool -q`, profile clone verified [ev:harness-cli-capabilities-b#F6][ev:harness-cli-capabilities-b#F19] | documented [ev:harness-cli-capabilities-b#F22] | documented native [ev:claude-agent-teams-hermes-openbot#F30]; not probed |

Not covered by hosted CI: live subscription login, provider availability, or model behavior. OpenClaw/Telegram remain outside the first pass; Hermes is deferred unless later selected. Host OS and harness availability stay independent invocation dimensions [ev:m0-product-architecture-review-2026-08-22#F5][fitgap:XC-02].

## 7. Historical order/stop rules (not approved sequencing)

**Historical order only.** The sequence formerly listed here is not approved. A replacement plan must define its own deterministic tier, credentialed tier, gates, and milestone-specific CI jobs while including all three first-pass harnesses.

**Stop criteria per PoC.** PASS (all criteria), FAIL-HARD (a criterion unmet after ≤3 Tier-1 cycles, or a falsification rule fires), or BUDGET (cap reached). Every stop writes a one-page result with ledger references; nothing proceeds past FAIL-HARD without review.

| Result | Consequence |
|---|---|
| Codex `-c developer_instructions` not honoured, `AGENTS.md` works | Run 1 passes `degraded`; answer unchanged [model:architecture-options#6] |
| No Codex channel at all | HB-02 falsified for Codex → HarnessAdapter recipe row of §1 → **back to architecture-options** |
| `defhash` changes | PoC FAIL; find the writer (symlinked skills, harness memory); injection fix, not architecture |
| Synthesis cannot attribute | Ensemble record/schema fix; layer-internal |
| Codex `exec` Member cannot run under `spawn subprocess -- ats-wrap` | either Members launched by `direct` and registered as `backend: subprocess` + pid [panel:judge-1.md#5] — a seam exception, since the layer would then *write* `spawn_registry.json` against assumption (g) (read only) [model:architecture-options#5] — or O6 vendoring of store/transport; both → **back to architecture-options** |
| Lead fails to drive the DAG on both Claude Code and Codex (tool-call non-compliance, ATM U2 [ev:atm-salvage#F24]) | not architecture: structured `ats`/`clawteam` calls, DAG state; if persistent → reliability as HarnessProfile dimension (HB-01), owner decides a Lead tier (R1) |
| Hidden Member visible on ClawTeam's board only | two rosters accepted; if the owner rejects (Q3) → `TeamMember.hidden` PR or spawn-path ownership [model:architecture-options#6] |
| Per-run data dir breaks `task wait`/inbox | record `namespace`; mechanical waits for ACL PR/spawn path [model:architecture-options#6] |
| Inbox signal not consumed, or `task update --status completed` refused, with real Members | carrier change inside the nesting contract (the `run.json` record stands; e.g. CodeMod polls the outer `run.json`, or the close runs with CodeMod's identity/`--force`); layer-internal |
| Windows smoke fails on the CLI path | in-process `SpawnBackend` (Python) or O6; seam unchanged [model:architecture-options#6] |
| ClawTeam bug the compiler cannot route around in B/C | CoordinationSubstrate swap (O6, dissent 3) → **back to architecture-options** |

## 8. Historical prerequisites and current question source

**Current prerequisites.** Implementation language, budget/deterministic-tier policy, synthesis, coordination protocol, Hermes scope, and ClawTeam PR timing remain open. Resolved: subscription OAuth for owner-operated native/unattended runs; credential-free hosted CI for Windows/macOS plumbing; bypass-visible/audited advisory PoC controls; ATM reuse authorization; and Claude Code + Codex + Grok Build in the first pass. See `.project-steward/QUESTIONS.md` for the authoritative open list.

**Open questions raised here.** (1) Does `spawn -n lead` into a team created by `spawn-team … --agent-name lead` reuse the member record? (Tier 0.) (2) May the Lead be re-invoked fresh per DAG event instead of one long headless invocation (TE-02 spirit; cost and compliance)? (3) Do the `inbox send` signal and the `task update --status completed` close of the owner's carrier work with live Members on the first PoC C run (including the task-lock question: close with CodeMod's identity env or `--force`)? — the only carrier question left; `task update --metadata` does not exist (§5). (4) Codex `--ephemeral` removes the rollout file — are thread id plus `codex --version` enough for HB-07?

STOP — this is a provisional M0 PoC proposal, not implementation authorization. Review and approve a re-baselined detailed plan before building.
