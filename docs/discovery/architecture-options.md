---
title: Architecture options — the smallest new software layer
status: draft v2.1 — M0 architecture direction retained; 2026-08-22 product-review constraints applied; detailed PoC shape provisional
date: 2026-08-22
owns: the single answer to "What is the smallest new software layer that genuinely needs to be built?" (§5:one paragraph + one table); the owner assumptions the answer adopts (§5.0) and its M-row coverage (§5.1); comparison of the eight options O1–O8 on the shared rubric R1–R10; panel results and dissent; where the HarnessBroker runs; the scheduler substrate for operational mode; risks that would change the answer; owner questions raised by the panel
depends_on: product-intent.md (register v3.3, historical PoC sketches §4); existing-systems-fit-gap.md (cells, roll-ups, G-ids); reuse-vs-build-analysis.md (rungs, PR set, licence table); assistant-domain-model.md; team-execution-model.md; harness-broker-model.md; legacy-atm-disposition.md; evidence/*.md; panel records evidence/panel/{proposal-A-clawteam-maximalist,proposal-B-independent-layer,proposal-C-upstream-extension,judge-1,judge-2}.md (cited as [panel:…]); the owner tiebreak note of 2026-08-22, complete at evidence/panel/owner-tiebreak.md (assumptions (a)–(l) enumerated in §5.0)
---

# Architecture options

> Reading guide. This document ranks options and states the current architecture direction; it re-authors no requirement (IDs from `product-intent.md` §3), re-classifies no fit cell (`existing-systems-fit-gap.md`) and re-decides no reuse rung (`reuse-vs-build-analysis.md`). §5 is self-contained so its answer can be copied into `README.md` and restated in `minimal-poc-plan.md`. The panel/tiebreak record remains a dated historical decision. The 2026-08-22 amendments in §5.0.1 supersede only the explicitly named operational assumptions; detailed PoC runs, synthesis, schemas, language, sequencing, and LOC remain provisional.

## 1. Question, method and rubric

**The question.** `product-intent.md` §6 asks discovery to answer one thing: *what is the smallest new software layer that genuinely needs to be built* — given ClawTeam, its OpenClaw fork, OpenClaw, Claude Code, Codex, Grok, Hermes and the reference designs — so that a reviewer can accept or reject it and the project stops for review.

**Current installed baseline (this host, 2026-08-22).** Claude Code 2.1.241, Codex CLI 0.149.0, Grok Build 1.0.5, OpenClaw 2026.7.1-2, Hermes 0.20.4, and ClawTeam 0.3.0@`0119833` with `mcp<2` [ev:m0-product-architecture-review-2026-08-22#F1]. Older evidence remains a dated snapshot; any version bump re-runs the adapter interface smoke test.

**Rubric R1–R10 (shared by every panel member; 1–5, higher better; size S < 2k LOC, M 2–8k, L > 8k, newly written code only).** R1 new-code size · R2 upstream-merge / dependency risk (ClawTeam internals, fork divergence, unmaintained dependencies — upstream last commit 2026-05-09, `clawteam-mcp` broken by unpinned `mcp` [ev:clawteam-spawn-platform#F24][ev:clawteam-probe-log#F1]) · R3 cross-platform / tmux-free story (no tmux here; subprocess backend; `os.getuid()` bug [ev:clawteam-spawn-platform#F20]) · R4 pressure-test coverage (PoC A, PoC B, PoC C, evolution, long-running operational run, hidden Member audit; clean vs hack vs not at all) · R5 lock-in / reversibility · R6 licence incl. automation-flag/ToS caveats · R7 time-to-PoC on this machine · R8 12-month maintenance · R9 reuse-ladder alignment (XC-03: lowest rung that actually satisfies the M rows) · R10 coverage of M-priority rows, listing any M requirement the option cannot satisfy.

**How proposals and judges were combined.** Three deliberately biased architects each evaluated all eight options on R1–R10 and proposed a "smallest layer": A (ClawTeam maximalist) answered O2 — an `ats` package over the ClawTeam *CLI* [panel:proposal-A-clawteam-maximalist.md#4]; B (independent layer) answered O7 — a Core with two declared seams and several execution backends [panel:proposal-B-independent-layer.md#4]; C (upstream-extension-first) answered O2+O3 over ClawTeam's Python seams with O5 PRs filed [panel:proposal-C-upstream-extension.md#4]. Two judges scored the *architecture each proposal actually proposes* against the evidence, never against the proposals' own text: Judge 1 (requirements and evidence fidelity) gave A 40, B 41, C 31 [panel:judge-1.md#2]; Judge 2 (reuse ladder, cross-platform, lock-in, maintenance) gave A 39, B 37, C 33 [panel:judge-2.md#2]. Each independently endorsed a named composition: Judge 1 — "B's ownership model and two seams, built at A's scope, with C's caveats and C's PRs as optional accelerators" [panel:judge-1.md#5]; Judge 2 — "A's O2 scope with B's two seams declared; C's PR set filed as good citizenship; C's in-process SpawnBackend kept as the documented Windows fallback" [panel:judge-2.md#5]. The overlap — A's scope, B's two seams, C's PRs filed not depended on, ClawTeam-CLI as the only team substrate — is the composition the owner adopted; the in-process fallback is Judge 2's wording, which Judge 1 raises only in dissent [panel:judge-1.md#6]. Where the judges differed (Judge 1 Q9: smaller *scope* with B's seams, or smaller *LOC claim*?), the owner's tiebreak note of 2026-08-22 is authoritative: its ranking is in §2, its assumptions (a)–(l) are enumerated verbatim in §5.0 and adopted in §3 and §5, and the complete note is preserved at `evidence/panel/owner-tiebreak.md`. Consolidated option scores in §2 are the range across the three architects' option tables, corrected where a judge found the underlying claim false (§4).

## 2. The eight options compared

Consolidated rubric scores (range across A/B/C's option tables [panel:proposal-A-clawteam-maximalist.md#2][panel:proposal-B-independent-layer.md#2][panel:proposal-C-upstream-extension.md#2]; a single number means all three agreed):

| Option | R1 | R2 | R3 | R4 | R5 | R6 | R7 | R8 | R9 | R10 | Total | Size |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| O1 ClawTeam as-is + packages | 5 | 3 | 2 | 1–2 | 1–4 | 5 | 4–5 | 2–3 | 2–3 | 1–2 | 26–34 | none/S |
| O2 ClawTeam + thin composition layer | 3–4 | 3–4 | 3–4 | 3–5 | 3–4 | 5 | 4 | 3 | 4–5 | 3–5 | 35–42 | M |
| O3 ClawTeam + broker as CT backend | 3–4 | 2 | 3 | 2–3 | 2 | 5 | 3 | 2 | 3 | 2–3 | 27–29 | M |
| O4 ClawTeam + OpenClaw/Telegram Surface | 3–4 | 2 | 2–3 | 1–2 | 2–3 | 4–5 | 2 | 2 | 2–3 | 1–2 | 22–28 | S adapter + core |
| O5 upstream ClawTeam PRs | 3–5 | 1 | 3–4 | 2–3 | 2–3 | 5 | 1–2 | 1–2 | 2–3 | 2 | 24–29 | S (PRs) |
| O6 selective module reuse | 2–4 | 3–4 | 4 | 3–4 | 4–5 | 4 | 2–3 | 3–4 | 2–4 | 3–4 | 33–38 | S vendored … M–L if it replaces CT |
| O7 independent layer, many backends | 2–3 | 4 | 4 | 4–5 | 5 | 5 | 2–3 | 2–4 | 3–4 | 5 | 37–42 | M–L |
| O8 new implementation | 1 | 3–5 | 4 | 4–5 | 5 | 5 | 1 | 1–2 | 1 | 4–5 | 31–33 | L |

*Total = the range of each proposal's own total for that option (e.g. O2: C 35 · B 36 · A ≈42), not the sum of the column ranges — adding column minima/maxima gives a slightly different pair.*

**O1 — ClawTeam as-is + reusable Assistant packages (conventions + scripts).** *Shape:* an Assistant is a directory of Markdown; a TeamTemplate is a ClawTeam TOML template whose `AgentDef.task` strings carry the rendered persona and whose per-agent `command` names the harness [ev:clawteam-model#F13][ev:clawteam-probe-log#F16]; the Lead (Claude Code or Codex holding ClawTeam's Skill [ev:clawteam-model#F16]) brokers by reading a policy file and nests by the documented CLI composition [ev:clawteam-model#F22]; up to three scripts (renderer, `tee` wrapper, Hermes shim). *Not built:* any object, record, gate, overlay or Proposal. *tmux-free/Windows:* subprocess backend works here [ev:clawteam-probe-log#F12]; Windows hits `os.getuid()` under the default `skip_permissions`, has no keepalive/exit code, truncates multi-line `-p` prompts in cmd.exe [ev:clawteam-spawn-platform#F20]. *Cannot satisfy (M):* AD-02, AD-05, AD-07, TC-01, TC-05, TE-05 (no parent link/result contract, cleanup leaves processes [ev:clawteam-probe-log#F18][ev:clawteam-probe-log#F20]), TE-07 (DEVNULL, `exit_code: null` [ev:clawteam-spawn-platform#F21][ev:clawteam-probe-log#F14]), HB-01, HB-03 (precedence inverted [ev:clawteam-probe-log#F17]), HB-05 (system prompt dropped for Codex [ev:clawteam-spawn-platform#F6]), EV-02, EV-03, AR-01/02/03 [fitgap:AD-01][fitgap:TC-01][fitgap:TE-05][fitgap:TE-07][fitgap:HB-01][fitgap:EV-03][fitgap:AR-01]. *Ladder:* rung 1 — below the rung that satisfies the M rows [reuse:G-AD-01][fitgap:XC-03]. *Size:* S.

**O2 — ClawTeam + thin Assistant composition layer.** *Shape:* a small package owns the data objects no substrate has (Assistant, TeamTemplate, overlays, HarnessProfiles, policy, run/invocation records, Proposals) and *compiles* a TeamRun into ClawTeam CLI calls — `team spawn-team`, `task create --blocked-by`, per-Member `spawn subprocess … -- <wrapper> <rendered argv>`, `task wait`, `snapshot`, stop, `cleanup` — never `launch` (swallows errors, one profile for all, no DAG edges [ev:clawteam-probe-log#F15][ev:clawteam-probe-log#F23][ev:clawteam-spawn-platform#F12]); an explicit positional command suppresses ClawTeam's implicit profile [ev:clawteam-spawn-platform#F5], so the layer decides how the definition reaches each harness; a wrapper tees output and writes the HarnessInvocation. *Not built:* DAG, mailbox, liveness, worktrees, tmux, Surfaces, a fork, a second team substrate. *tmux-free/Windows:* `skip_permissions=false` in the layer-written config (`os.getuid()` sits inside `if skip_permissions:` [panel:judge-1.md#1]); prompts by file; wrapper-captured exit codes; unprobed off this host [fitgap:TE-08]. *Cannot satisfy:* TE-08 only documented; HB-02 Codex channel observed, not executed [ev:harness-cli-capabilities-a#F2]; TC-05 gate bypassable by a Lead calling `clawteam spawn` directly [ev:clawteam-spawn-platform#F16]; TE-05 isolation namespace-only [ev:clawteam-probe-log#F19]; AR-03 partial by design (metadata-only: manifest/lock/report, no installer — re-establishment on another host is a manual step). *Ladder:* rung 1/2 over ClawTeam, rung 6 only for the data objects listed in [reuse:§6] (the rung tally is reuse §2 "Reading across", not restated here) [reuse:G-XC-03]. *Size:* M.

**O3 — ClawTeam + HarnessBroker extension (broker as ClawTeam backend/plugin).** *Shape:* the broker packaged as a custom `SpawnBackend` registered through `register_backend`, receiving `prompt`/`system_prompt`/`command` [ev:clawteam-spawn-platform#F8]. *Not built:* definitions, templates, nesting, evolution. *Why it is a misnomer:* `register_backend` is process-local, `clawteam spawn` never loads plugins, and `BeforeWorkerSpawn` neither fires on the CLI paths nor can mutate [ev:clawteam-spawn-platform#F16][ev:clawteam-spawn-platform#F17] — so the entry point must be our own process importing "Alpha" seams with no `__all__` [ev:clawteam-spawn-platform#F15]; and ClawTeam's `is_agent_alive`/`stop_agent` handle only `tmux|subprocess|wsh` names, so a custom backend is never seen alive or dead and its task locks are never releasable [panel:judge-1.md#1][ev:clawteam-probe-log#F8]. The broker is part of the layer, not a ClawTeam backend. *Windows:* an in-process backend never calls `prepare_command` — no `os.getuid()`, no argv prompt — the one genuine advantage, Python-only. *Cannot satisfy alone:* AD, TC, TE-05, EV, AR M rows. *Ladder:* rung 2/3 on the least stable seam where the CLI seam satisfies the same rows. *Size:* M.

**O4 — ClawTeam + optional OpenClaw/Telegram Surface.** *Shape:* O1/O2 unchanged plus a Surface adapter presenting a TeamRun's *visible* Members over OpenClaw channels (`agent --deliver` [ev:clawteam-openclaw-fork-delta#F5]; bindings and per-agent `identity`, where the channel account — not the `agentId` — is the visible sender [ev:openclaw-native-and-telegram-verification#F20]) or Hermes gateway platforms with profile routing (`hermes peer dm`; no Hermes `send` command is evidenced) [ev:claude-agent-teams-hermes-openbot#F26]. *Not built:* anything that closes an M gap. *Windows:* Gateway daemon per host; WSL2 recommended [ev:harness-cli-capabilities-b#F16]. *Cannot satisfy:* whatever its core cannot; MS-01/MS-04 are already satisfied without it [fitgap:MS-01][fitgap:MS-04]; MS-02 is C-priority; the PoCs need neither OpenClaw nor Telegram (product-intent §4). *Ladder:* rung 2 for a C row. *Size:* S adapter plus two daemons' release trains and OpenClaw flag drift [ev:clawteam-openclaw-fork-delta#F5].

**O5 — upstream ClawTeam extensions (PRs only).** *Shape:* the nine bounded PRs of assumption (k) (§5.0; enumerated under "Deferred" in §5) — eight of the nine rows of [reuse:§5] (the pre-spawn mutation hook was dropped by C itself once the broker renders before spawn), with reuse §5's guard + `mcp<2` pin row counted as two. *Not built:* our objects. *Windows:* the guard/logs PRs are exactly the Windows fixes, if merged. *Cannot satisfy:* every object O1 lacks; and R2 = 1 — HEAD `0119833` unchanged since 2026-05-09, 22 open PRs, #159/#165/#167 unanswered [ev:clawteam-spawn-platform#F24]; #82/#88 open since 2026-03-24 (re-verified by both judges via `gh` on 2026-08-22 — not in the evidence corpus) [panel:judge-1.md#1][panel:judge-2.md#3]. *Ladder:* rung 3, which reuse §2 reads as "never the only path" — every item has a rung-2 shadow [reuse:G-XC-03]. *Size:* S (~0.4–0.7k incl. tests).

**O6 — selective reuse of existing modules into a new small layer.** Two readings exist in the panel. *Reading 1 (A):* vendor ClawTeam's task store/mailbox/registry and *replace* ClawTeam — M–L, rung 4 where rung 1–2 satisfies TE [fitgap:TE-01]. *Reading 2 (B; adopted here):* a sourcing rule, not a layer [panel:proposal-B-independent-layer.md#2] — vendor bounded MIT helpers with attribution, and take Hermes/DG/ATM shapes as patterns. ATM copy/adaptation is internally authorized, with provenance and third-party notices preserved [ev:m0-product-architecture-review-2026-08-22#F7]. *Cannot satisfy alone:* it needs O2/O7 to say what the layer is. *Size:* S vendored.

**O7 — a small independent Assistant/Harness layer above multiple execution backends.** *Shape:* O2's objects plus two seams (HarnessAdapter; CoordinationSubstrate) and several backends: ClawTeam, direct CLI, Claude Code native subagents/teams, OpenClaw sessions, Hermes kanban [panel:proposal-B-independent-layer.md#4]. *Not built:* DAG, mailbox, liveness, UI, Surfaces. *Windows:* the `direct` launcher (own `subprocess`, `shell=False`, file-delivered prompts) is the cleanest tmux-free path for solo runs and ensemble legs. *The evidence on the extra backends:* Claude Code teams are experimental, Claude-only, interactive-only, non-nested [ev:claude-agent-teams-hermes-openbot#F2][ev:claude-agent-teams-hermes-openbot#F6][ev:claude-agent-teams-hermes-openbot#F7][ev:claude-agent-teams-hermes-openbot#F8]; OpenClaw `sessions_spawn(runtime:"acp")` needs the Gateway and the absent acpx plugin; its ACP config carries `agent/backend/cwd/mode` only, so persona delivery to an ACP child is unevidenced (`?`) and native sub-agent children receive only `AGENTS.md` + `TOOLS.md` [ev:openclaw-native-and-telegram-verification#F16][ev:openclaw-native-and-telegram-verification#F14][ev:harness-cli-capabilities-b#F12]; Hermes kanban is Hermes-only and un-nestable [ev:claude-agent-teams-hermes-openbot#F25]. None can host a mixed-harness nested TeamRun today [fitgap:TE-03][fitgap:TE-05]; B concedes "multiple backends is thinner than the slogan" [panel:proposal-B-independent-layer.md#8]. *Cannot satisfy:* TE-08 docs-only; HB-02 Codex; AD-05/EV-04 heuristic. *Ladder:* rung 2 + 6 like O2, plus a seam built before a second team substrate exists (XC-03 bent, not broken). *Size:* M–L (B's table sums ≈11.6k; most of the excess is scope the register does not demand [panel:judge-1.md#3]).

**O8 — a new implementation.** *Shape:* own DAG, mailbox, registry, spawn, archive. *Why not:* the TE layer is "not genuinely new — reachable at reuse rung 1–3" [fitgap:TE-01]; ClawTeam's DAG/inbox/registry are `S!` probe-verified [ev:clawteam-probe-log#F7][ev:clawteam-probe-log#F9][ev:clawteam-probe-log#F13]; ATM's lesson: "do not build a mailbox runtime" [ev:atm-salvage#F22]. *Ladder:* rung 6 with rung 1–2 available — violates XC-03. *Size:* L; months to a PoC.

**Ranking (owner tiebreak, adopted):** O2 + seams (the composition of §5) > O7 (B) > O5 filed-not-depended-on > O6 selective vendoring (`platform_compat.py`, `model_resolution.py` pattern, attribute HKUDS) > O3 (misnomer: the broker is part of the layer, not a ClawTeam backend) > O1 (fails M rows EV-03, AD-05, TC-05, TE-05, HB-01/03) > O4 > O8 (not justified by evidence).

## 3. Pressure-test walkthroughs under the recommended layer

> **Provisional scenarios, not the next implementation plan.** These walkthroughs explain architecture pressure points from M0. Their run count, harness allocation, synthesis path, schemas, order, and acceptance details must be re-baselined. Any first-pass plan must include Claude Code, Codex, and Grok Build [ev:m0-product-architecture-review-2026-08-22#F3].

All eight walk on the composition of §5 (mechanics → new vs reused → evidence) under the owner's assumptions (a)–(l) of §5.0; the three that carry the mechanics are: (b) the layer never passes `--task` through `clawteam spawn` — the wrapper delivers the whole bundle (persona, principles, coordination section) from a file and `skip_permissions=false` is written into the layer's ClawTeam config, so no multi-line prompt transits argv on any OS (ClawTeam builds its prompt only when `task` is given and the generic adapter branch appends `-p` only `elif prompt:` [panel:judge-1.md#1][ev:clawteam-probe-log#F12]); (c) every `clawteam spawn subprocess` exit, clean or not, runs `lifecycle on-exit` (task reset + "exited unexpectedly" to the ClawTeam leader; `--no-keepalive` does not suppress it) [ev:clawteam-probe-log#F14][panel:judge-1.md#1], so one-shot legs use the `direct` launcher and register nothing in ClawTeam; (g) ClawTeam state is read through CLI `--json` and `team snapshot`; the only internal file read is `spawn_registry.json` (pids for stop-before-cleanup) [ev:clawteam-probe-log#F13][ev:clawteam-probe-log#F20].

**PoC A — historical M0 sketch: same Assistant on Codex / Claude Code / both + synthesis.** *Mechanics:* `ats run --assistant code-reviewer --harness codex` on the `direct` launcher (no ClawTeam team). The overlay resolver produces the effective definition + hash; the HarnessBroker resolves policy and prepares one injection bundle. The Codex sketch uses `codex exec ... --ephemeral`; the Claude sketch must now use `claude -p --safe-mode --no-session-persistence ...` because `--bare` disables subscription OAuth [ev:m0-product-architecture-review-2026-08-22#F3]. The wrapper records outcome/usage and an Ensemble record links legs and synthesis. This sketch does **not** define a first-pass plan: Grok Build must be included, and run count/synthesis/acceptance details remain provisional.

**PoC B — fresh TeamRun, hidden temporary Member, mixed harnesses.** *Mechanics:* `ats run development --repo R --task T` → the run layer resolves three Assistants, the broker binds a harness per Member, `run.json` is written before any process starts [model:team-execution-model#3]; the ClawTeam adapter (`CLAWTEAM_DEFAULT_BACKEND=subprocess`, optional per-run `CLAWTEAM_DATA_DIR` [ev:clawteam-probe-log#F3]) runs `team spawn-team run-X -n lead` first (the first spawn into a new team becomes ClawTeam's leader [ev:clawteam-model#F5]), `task create --blocked-by` from the workflow skeleton (ids validated by the layer [ev:clawteam-probe-log#F7]), then per Member `spawn subprocess --team run-X -n <m> -- ats-wrap <invocation>` with fully rendered argv (Lead on Claude Code, Implementer on Codex, Reviewer on Codex or Hermes `chat --source tool -q … -p <cloned profile>` [ev:harness-cli-capabilities-b#F6][ev:harness-cli-capabilities-b#F19]; per-Member command by configuration is probe-verified [ev:clawteam-probe-log#F16]). Mid-run the Lead calls `ats member add --hidden --from-ephemeral migsafe.md`; the gate checks `dynamic_members {may_create, max_count, allowed_harnesses}` *before* the substrate spawn, writes the Ephemeral Assistant into the run directory and records `visibility: hidden, origin: ephemeral, policy_decision` [model:team-execution-model#5]. `ats status` filters hidden, `ats archive` lists all; completion via `task wait run-X --agent <observer>` (explicit `--agent` avoids draining the leader inbox [ev:clawteam-model#F8]). *New:* run record, roster projections, gate, Ephemeral Assistant writer, wrapper. *Reused:* ClawTeam team/task/inbox/registry/snapshot. *Honesty (assumptions (d), (f)):* two rosters — ClawTeam's `team status`/`board show` still list `migsafe` [ev:clawteam-probe-log#F10], so PoC B's "user-facing member list" is the layer's view; the achievable TC-05 level on ClawTeam today is "gate + convention + run-scoped `CLAWTEAM_BIN`/PATH shim that refuses raw `spawn` inside a run + post-hoc roster/registry reconciliation" (the owner's words; "reconciliation" here is a post-hoc audit diff of `spawn_registry.json` against the roster, not ATM's desired-state reconciliation) — the Lead *can* bypass it by calling `clawteam spawn` directly (no pre-spawn hook on the CLI path [ev:clawteam-spawn-platform#F16]); mechanical enforcement needs the upstream hook PR or owning the spawn path.

**PoC C — nested TeamRun.** *Mechanics:* CodeMod, a Member of `ops`, runs `ats nest development --for-task t4`; the run layer checks `nesting {allowed, max_depth}`, creates an isolated inner run/ClawTeam namespace, records the parent link, and uses subscription-compatible fresh contexts (`--safe-mode --no-session-persistence` for Claude, `--ephemeral` for Codex, isolated profile/home controls for other harnesses). Worktree/base-branch and achieved-isolation details remain pressure-test questions. *New:* nesting contract. *Reused:* ClawTeam team-as-namespace and snapshot.

**Nested TeamRun result + archive.** *Mechanics:* the inner Lead ends with `ats result`; the run layer writes the RunResult into the outer `run.json` and inner archive, signals the creator with `clawteam inbox send`, closes the delegated task with `task update --status completed`, snapshots, stops processes before cleanup, and links the nested archive. There is no ClawTeam CLI metadata carrier. Fresh-context checks use subscription-compatible isolation rather than Claude `--bare` [ev:m0-product-architecture-review-2026-08-22#F3].

**Ensemble + synthesis (generalised).** *Mechanics:* `ensemble {legs: [claude-code, codex], min_legs: 2, synthesis_harness: policy}` on any Member/task; each leg a HarnessInvocation with `ensemble_id`, own workspace and its profile's isolation knobs; synthesis a recorded third invocation with `inputs: [leg ids]` [model:harness-broker-model#8]. Legs run on `direct` (ClawTeam fan-out is probe-verified [ev:clawteam-probe-log#F16] but every spawn would trigger on-exit noise, assumption (c)). Attribution comes from the ledger, never from ClawTeam's self-reported `CostEvent` [ev:clawteam-model#F28][fitgap:HB-05][fitgap:HB-07]. *New:* Ensemble record, bundle identity, structured verdict, `cost_source` per leg. *Reused:* per-harness output schemas [ev:harness-cli-capabilities-a#F10].

**Evolution overlays + Proposals.** *Mechanics:* `ats propose --from run-…` (after PoC C, assumption (i)) reads archives (findings, Lead corrections, invocation outcomes) and emits bounded, typed Proposals with provenance, `independence_check` (paths, branches, session ids, topics, secrets) and dedupe; `ats review` accepts/rejects, appending to the Reviewed Evolution Overlay with `from_proposal`, `runs`, `approved {by, at, applied_hash}`; the resolver's merge Base → Reviewed Evolution → User changes the effective-definition hash that every later invocation records; `persona/purpose` edits and permission widening are rejected by the per-field policy [model:assistant-domain-model#9]. *New:* overlay document, Proposal + decision record, merge policy, resolver — all data-level; no substrate has any [fitgap:EV-02][fitgap:EV-03]. *Reused:* nothing executable; ClawTeam's plan round-trip is an optional approval transport [ev:clawteam-model#F17]. Only the resolver is in the PoC-A slice.

**Long-running operational run with deterministic backends.** *Mechanics (later, S-priority):* a deterministic scheduler wakes a fresh Assistant invocation and records trigger → invocation → decision → action. Native unattended invocations use subscription OAuth on the owner's persistent host; no hosted CI credential and no `--bare` for Claude. The exact scheduler/backend model remains outside the first-pass PoC plan [ev:m0-product-architecture-review-2026-08-22#F2][ev:m0-product-architecture-review-2026-08-22#F5].

**Ephemeral hidden Member audit.** *Mechanics:* the archive roster entry `{name, origin: ephemeral, visibility: hidden, created_by, policy_decision, definition_ref (full rendered text), invocation}`; the invocation record `argv_redacted, cwd, env_names, bundle_hash, stdout path, harness session id, usage, exit code` (the wrapper tees — ClawTeam alone discards output and writes `exit_code: null` [ev:clawteam-spawn-platform#F21][ev:clawteam-probe-log#F14]); ClawTeam's `spawn_registry.json` (pid, argv) and `events/` mirror are referenced [ev:clawteam-probe-log#F13][ev:clawteam-probe-log#F9]. Reconstruction test: rebuild the exact bundle and command from the archive alone. User view omits hidden unless `--all`; promotion to a persistent Assistant is a separate human action through the validator [model:assistant-domain-model#8]. *New:* roster projections + wrapper. *Reused:* registry/events.

## 4. Panel results and dissent

**Per-proposal scores.** Judge 1 (requirements & evidence): A 40 · B 41 · C 31; Judge 2 (reuse, platform, maintenance): A 39 · B 37 · C 33 [panel:judge-1.md#2][panel:judge-2.md#2]. Both endorsed the same composition (§1). Judge 1's sizing note is adopted: the three component tables list the *same* ≈12 objects; the LOC spread (A ≈4k, C ≈6–8k, B ≈9–11k) is estimation variance and chosen scope, so R1 is scored on scope [panel:judge-1.md#1].

**Factual errors the judges found, and the synthesis' verdict.**

| Proposal | Claim | Judge finding | Accepted here? |
|---|---|---|---|
| A | explicit positional command "bypasses ClawTeam's adapter chain" | only the profile half is right: `prepare_command` still runs; an unrecognised basename falls into the generic branch and gets `-p <prompt>` appended when a prompt exists; `os.getuid()` runs whenever `skip_permissions` is true [panel:judge-2.md#3] | **yes** — hence assumption (b) |
| A, B | `--no-keepalive` makes one-shot legs quiet | false: the POSIX wrapper always appends `lifecycle on-exit` (`keepalive.py:64-69`) [panel:judge-1.md#1] | **yes** — hence assumption (c) |
| A, B | prompts travel by file (Windows) | true only on the `direct` path; `--task` through `clawteam spawn` still puts ClawTeam's multi-line prompt on the cmd.exe argv (PR #159 truncation) [panel:judge-1.md#3][panel:judge-2.md#3] | **yes** — fixed by (b) |
| A | O2 ≈3–4k LOC vs own table ≈4.35k | inconsistent [panel:judge-1.md#3] | **yes** — §5 sizes by slice |
| B | per-run `CLAWTEAM_DATA_DIR` = "mechanical" isolation | default isolation only; the global `--data-dir` flag exists on every command [panel:judge-1.md#3] | **yes** — `independence.achieved: namespace | data-dir | mechanical` (vocabulary and attribution in §8) |
| B | size table sums ≈11.6k vs "9–11k"; five adapters counted into the PoC core | R1 should be 2 [panel:judge-2.md#3] | **yes** — PoC-A slice has two adapters |
| C | a custom `register_backend("ats")` backend is seen by ClawTeam's registry/liveness | false: `is_agent_alive`/`stop_agent`/dead-agent listing handle only `tmux|subprocess|wsh`; locks held by such a Member are never releasable [panel:judge-1.md#1] | **yes** — liveness stays on ClawTeam's own backend names; in-process backend is a fallback only |
| C | `keepalive=False` suppresses on-exit for operational invocations | false in ClawTeam; only C's own wrapper can [panel:judge-1.md#3] | **yes** |

Minor and not material: B's three wrong citation anchors (right facts) and C's ranking being inconsistent with its own cells [panel:judge-1.md#3][panel:judge-2.md#3]; corrected anchors are used here.

**Coverage gaps shared by all three** (adopted as PoC preconditions or honesty labels): Codex `-c developer_instructions` unexecuted [ev:harness-cli-capabilities-a#F2]; Windows/macOS never probed, no Windows CI anywhere [fitgap:TE-08]; nested result carrier unverified with live Members (now fixed as `run.json` record + `inbox send` + `task update --status`, §3); Member-side coordination text is ClawTeam-Skill text — the lock-in none priced (owner question §7); synthesis harness and Codex cost derivation undecided [model:harness-broker-model#13]; Lead tool-call compliance risk (ATM U2 0/2 [ev:atm-salvage#F24]) with a deterministic no-op-harness tier named only by B; TC-05 bypass unflagged by A and B [panel:judge-2.md#3].

**Dissent (kept honest — what a reasonable colleague still argues).**
1. **B outright.** The seams are not abstraction before need: the only team substrate is dormant and its packaging already broke once (`mcp` 2.0 [ev:clawteam-probe-log#F1]); B's `direct` launcher gets PoC A done without installing ClawTeam; the extra ≈2k is cheaper than the first forced migration [panel:judge-2.md#6][panel:judge-1.md#6]. *Answered by:* both seams declared day one at A's scope (§5).
2. **C's in-process route.** The only design in which ClawTeam's registry, liveness and `lifecycle on-exit` see the process *and* the argv hazards vanish on Windows; CLI-driving a Python package from another process is the real hack; a dead upstream's Alpha seams will never move [panel:judge-2.md#6][panel:judge-1.md#6]. *Answered by:* kept as the documented Windows fallback (§5, §6).
3. **O6 against ClawTeam entirely.** The PoCs need a DAG, an inbox and a pid registry; vendoring `store/file.py` + `transport/file.py` (~300 lines, MIT) drops a dormant dependency with a broken MCP server and unfixed bugs; ATM's "do not build a mailbox runtime" warns against writing one, not copying 300 lines [panel:judge-2.md#6]. *Answered by:* the CoordinationSubstrate seam makes this a one-implementation swap if PoC B hits a bug the compiler cannot route around (§6).
4. **O1 first as a one-day zero-code demo.** A TOML template with `claude` and `codex` members plus a `tee` wrapper would settle within hours whether ClawTeam's subprocess path carries a Codex Member at all [ev:clawteam-spawn-platform#F1][panel:judge-1.md#6]. *Answered by:* the day-one probes of assumption (j) settle the same without committing to ClawTeam's argv path.
5. **Hermes profiles + kanban as the cheapest substrate for single-harness operational (LO) teams.** Fresh-per-task workers, durable runs and `--usage-file` cost records exist there [ev:harness-cli-capabilities-b#F19][ev:claude-agent-teams-hermes-openbot#F25]; ClawTeam's resident-worker model is the inverse of LO [ev:clawteam-model#F15]. *Answered by:* operational mode is deferred, Hermes is a first-class Harness (assumption (h)) and the scheduler substrate is pluggable; if the owner's first real use is training operations this dissent rises (§6).

## 5. The answer

> Owner tiebreak (2026-08-22): both judges independently endorsed the same named composition ("A's O2 scope with B's two seams", §1); the session owner confirmed it as THE answer and fixed assumptions (a)–(l), enumerated verbatim in §5.0 below; the complete note is preserved at `evidence/panel/owner-tiebreak.md`.

### 5.0 Owner assumptions adopted (verbatim; owner-authored)

The twelve assumptions below are quoted one-for-one from the owner's tiebreak note of 2026-08-22 (`evidence/panel/owner-tiebreak.md`, "ASSUMPTIONS the synthesis must adopt verbatim"); they are the only owner-authored constraints in this document — every other sentence is synthesis-authored. Where a later owner decision amends one, the amendment is bracketed.

- **(a)** language-agnostic CLI coupling; implementation language is an owner question;
- **(b)** never pass --task through 'clawteam spawn' — the wrapper delivers the bundle from a file; skip_permissions=false in the layer-written ClawTeam config;
- **(c)** every ClawTeam spawn runs lifecycle on-exit on exit — one-shot legs use 'direct' and register nothing;
- **(d)** TC-05 is 'gate + convention + run-scoped CLAWTEAM_BIN shim + post-hoc roster/registry reconciliation' until enforced — label it so in R10;
- **(e)** TE-05 isolation level is RECORDED per nested run (independence.achieved: namespace | data-dir | mechanical), result carrier = task update --metadata AND inbox send, settled in PoC C; *[amended by the owner's fix-pass decision of 2026-08-22: `task update --metadata` is not a CLI verb in ClawTeam 0.3.0@0119833 (`ClawTeam/clawteam/cli/commands.py:2181`); the record carrier is the outer run's `run.json` + the inner archive, the signal is `inbox send`, and the delegated task is closed by `task update --status completed` — §3 "Nested TeamRun result + archive"; the isolation vocabulary stands, its provenance is in §8]*
- **(f)** two rosters accepted for PoC B (layer views vs ClawTeam board), stated as such;
- **(g)** read ClawTeam state via CLI --json/team snapshot; the only internal file read is spawn_registry.json (pids);
- **(h)** Hermes = first-class Harness and AR-03 package precedent, not a team substrate;
- **(i)** size by slice: PoC-A slice first (schemas + overlay resolver + claude-code & codex adapters + broker resolve/record + launcher/wrapper + ledger + ensemble record, ≈2–3k LOC), run layer gated on PoC A passing, operational mode + proposal generator after PoC C; reconcile A's 3–4k / B's 9–11k / C's 8k estimates against the slice plan, and present the total as 'M (≈4–6k LOC incl. data) with the PoC-A slice ≈2–3k';
- **(j)** day-one probes are PoC preconditions: codex exec -c developer_instructions honoured + positional prompt without TTY; claude -p --bare --append-system-prompt-file under the wrapper; Hermes --source tool hiding/memory on 0.20.4 if a Hermes Member is used; one Windows + one macOS smoke (wrapper + one clawteam spawn subprocess);
- **(k)** file C's nine bounded PRs as intent (getuid guard, mcp<2, logs+exit code, created_by parent link, TeamMember.hidden, cleanup stops processes, Hermes/Grok branches + Codex/Grok system prompt, launch error surfacing + AgentDef.profile + TaskDef.blocked_by, inbox membership ACL) — plan as if none merges;
- **(l)** Codex cost attribution: synthesis record carries cost_source: unavailable|derived.

### 5.0.1 Current product-review amendments (2026-08-22)

These constraints supersede the dated assumptions above only where they conflict:

- The first-pass harness set is Claude Code + Codex + Grok Build; Hermes and OpenClaw are deferred. Assumption (i)'s two-adapter slice, run count, gating sequence, synthesis choice, and ≈2–3k slice estimate are provisional.
- Native and unattended live runs use each CLI's subscription OAuth on an owner-operated persistent host. Claude subscription isolation uses `--safe-mode --no-session-persistence`, not `--bare`. API-test providers are separate, environment-keyed, replaceable profiles and never authentication fallbacks.
- GitHub-hosted Windows/macOS CI verifies deterministic direct-path plumbing at its milestone and ClawTeam subprocess plumbing at its milestone. It carries no live credentials and proves neither live authentication nor model behavior.
- Advisory controls are acceptable in a PoC when bypassability is visible and audited; production claims require mechanical enforcement. UI hiding is a projection, not an access-control claim.
- ATM is owner-authorized for internal copy/adaptation; preserve provenance and third-party notices. This permission does not select ATS's public licence.
- OpenRouter `stealth/ox-alpha` is a temporary API-test-only model. Its canary is deferred; no availability, cost, quality, or native-harness equivalence is claimed.

[ev:m0-product-architecture-review-2026-08-22#F2][ev:m0-product-architecture-review-2026-08-22#F3][ev:m0-product-architecture-review-2026-08-22#F4][ev:m0-product-architecture-review-2026-08-22#F5][ev:m0-product-architecture-review-2026-08-22#F7]

Synthesis-authored decisions that sit next to these and are *not* in the note: the ClawTeam-adapter mechanics of §3 (no `--task` is (b); the unrecognised wrapper basename and the generic-branch reading of `adapters.py:131-140` are the judges' [panel:judge-1.md#5][panel:judge-2.md#5]); the `independence.achieved` provenance (§8); the broker location and scheduler substrate (§8); the PoC C base-branch mechanism (§3); the M-row coverage labels of §5.1.

**The smallest new software layer is** a thin, format-independent Assistant/Team layer — working name `ats` — that owns only the data no existing system has and reaches everything else through two declared seams. It owns, as substrate-neutral data: Assistant definitions (closed schema + exclusion validator), TeamTemplates composing Assistants by reference, Base/User/Reviewed-Evolution overlays with a deterministic resolver and a Proposal/review record, HarnessProfiles plus a HarnessSelectionPolicy resolved per invocation (user > Assistant > team > default), a HarnessInvocation ledger and an Ensemble record with per-invocation attribution, an artifact manifest + lock + per-host resolution report, and a TeamRun record with user-visible and archive rosters, a dynamic-member gate, a nesting contract, and an archive. It declares **HarnessAdapter** {profile data, injection recipe, invoke, parse} and **CoordinationSubstrate** {create_space, add_member, create/update/wait task, send/receive, snapshot, stop, cleanup}. Five harness targets remain planned; Claude Code, Codex, and Grok Build are required in the first pass, while Hermes and OpenClaw are deferred. The single planned CoordinationSubstrate implementation is a ClawTeam CLI adapter (pinned 0.3.0@0119833, `mcp<2`, subprocess backend, no tmux, never `launch`, never `--task` through `spawn`, `skip_permissions=false`), plus a small `direct` launcher for solo runs and independent harness invocations. The HarnessBroker lives inside this layer, not as a ClawTeam backend or plugin. The layer builds no second team substrate, ClawTeam fork, library-seam coupling, Surface adapter, artifact installer, UI, message bus, task store, or liveness registry. ClawTeam supplies the task DAG, inboxes, pid registry, worktrees, and snapshot; harnesses supply execution. **M (≈4–6k LOC including profile data) remains a discovery estimate; the first-pass slice, detailed PoC runs, synthesis design, implementation language, sequencing, and slice LOC must be re-baselined before implementation.**

| Component | Responsibility | Size | What it reuses (substrate primitive) | Evidence |
|---|---|---|---|---|
| Assistant definition package | closed schema (unknown keys rejected), exclusion/content validator, `id@version` + content hash | S ~0.4k | **new** data; shape ideas DG `.strict()`, Hermes `distribution.yaml` | [reuse:G-AD-01][reuse:G-AD-05][ev:dsh-agent-teams-and-gui#F20][ev:claude-agent-teams-hermes-openbot#F24] |
| TeamTemplate by reference | members/Lead/relationships, independence declaration, visibility defaults, dynamic-member policy, team preferences, workflow skeleton | S ~0.3k | **new** data; rendered into ClawTeam calls at run time | [reuse:G-TC-01][reuse:G-TC-05][model:team-execution-model#2] |
| Overlay resolver + Proposal/review record | Base → Reviewed Evolution → User merge with per-field policy + hash; Proposal with provenance, independence check, `applied_hash` | S ~0.4k (resolver in PoC-A slice; generator/review CLI deferred) | **new**; Hermes two-layer ownership split as precedent; ClawTeam plan round-trip optional transport | [reuse:G-EV-02][reuse:G-EV-03][model:assistant-domain-model#9][ev:clawteam-model#F17] |
| HarnessProfiles + HarnessSelectionPolicy resolver | capability rows with verification levels; resolve user > Assistant > team > default; fallback = new invocation | S ~0.4k + data | **new** data model; seeded from the evidence checklists; CTF `model_resolution.py` precedence pattern (rung 4) | [reuse:G-HB-01][reuse:G-HB-03][ev:harness-cli-capabilities-a#F16][ev:harness-cli-capabilities-b#F23][ev:clawteam-openclaw-fork-delta#F15] |
| HarnessAdapter seam + 5 planned adapters | per harness: profile data, injection recipe (flags vs prepared dir, `degraded[]`), invoke, parse session id/usage/outcome | M discovery estimate; Claude Code + Codex + Grok Build required first pass; Hermes/OpenClaw deferred | wraps each harness's own flags; an explicit positional command suppresses ClawTeam's implicit profile [ev:clawteam-probe-log#F17] | [model:harness-broker-model#10][ev:m0-product-architecture-review-2026-08-22#F3][ev:harness-cli-capabilities-b#F24] |
| HarnessBroker + `direct` launcher/wrapper + ledger + Ensemble record | resolve → bundle + `bundle_hash` → invoke → tee stdout/stderr + exit code → record; fan-out + synthesis invocation | S–M ~0.6k | **new** loop and record; wrapper shape from CTF `subprocess_wrapper.py`; fan-out via `direct` (ClawTeam optional) | [reuse:G-HB-05][reuse:G-HB-07][ev:clawteam-spawn-platform#F21][ev:clawteam-probe-log#F14][ev:harness-cli-capabilities-a#F10] |
| Artifact manifest + lock + resolution report | capability vs artifact vocabulary; kinds; vendored/git sources; lock `{ref, resolved, digest, fingerprint}`; per-host native/installed/degraded/unsupported | S ~0.3k | **new** data; ATM ADR 0022 ideas; OpenClaw eligibility, Hermes `env_requires` as inputs | [reuse:G-AR-01][reuse:G-AR-06][ev:atm-salvage#F10][ev:harness-cli-capabilities-b#F11] |
| CoordinationSubstrate seam + ClawTeam-CLI adapter | create_space/add_member/task/wait/send/receive/snapshot/stop/cleanup over `clawteam team spawn-team`, `task create/update/wait`, `inbox send/receive`, `spawn subprocess -- ats-wrap …`, `team snapshot/cleanup`; state read via `--json`/snapshot; only `spawn_registry.json` read directly | S ~0.6k | wraps ClawTeam CLI (rung 2); DAG, inboxes, registry, worktrees, snapshot are ClawTeam's | [ev:clawteam-probe-log#F7][ev:clawteam-probe-log#F9][ev:clawteam-probe-log#F12][ev:clawteam-probe-log#F16][ev:clawteam-probe-log#F22][ev:clawteam-model#F22] |
| Run layer | `run.json`; roster projections (visible vs archive); dynamic-member gate + Ephemeral Assistant writer; nesting contract (parent link, delegated task, RunResult recorded in the outer `run.json` + inner archive, signalled by `inbox send`, delegated task closed by `task update --status`, `independence.achieved`, stop-before-cleanup); archive writer | M ~1.0k (gated on PoC A) | **new** objects over ClawTeam mechanics (no `task update --metadata` — not a CLI verb) | [model:team-execution-model#5][model:team-execution-model#6][reuse:G-TE-05][ev:clawteam-probe-log#F18][ev:clawteam-probe-log#F19][ev:clawteam-probe-log#F20] |
| CLI + Lead/Member Skill + `CLAWTEAM_BIN` shim | `ats assistant/template/run/member/nest/result/archive`; SKILL.md telling a local Claude Code/Codex conversation when to call `ats`; coordination section rendered per substrate into the bundle; run-scoped shim refusing raw `spawn` | S ~0.3k + docs | wraps ClawTeam's own Skill (rung 1) | [ev:clawteam-model#F16][fitgap:MS-01][ev:clawteam-spawn-platform#F16] |
| Vendored modules | `platform_compat.py` (pid_alive, locks, Windows defaults), `model_resolution.py` pattern, `spawn_with_retry`; attribute HKUDS | S ~0.2k copied | rung 4, MIT | [ev:clawteam-openclaw-fork-delta#F19][ev:clawteam-openclaw-fork-delta#F28][ev:clawteam-openclaw-fork-delta#F30] |

**Explicitly not built**
- No second team substrate: no Claude Code teams, OpenClaw sessions or Hermes kanban substrate — none can host a mixed-harness nested TeamRun today [fitgap:TE-03][fitgap:TE-05][ev:claude-agent-teams-hermes-openbot#F7][ev:claude-agent-teams-hermes-openbot#F25][ev:openclaw-native-and-telegram-verification#F21].
- No ClawTeam fork; no library-seam coupling (`TeamManager`/`FileTaskStore`/`MailboxManager`/`SpawnBackend` imports) — C's in-process `SpawnBackend` is the documented Windows fallback only if Python is chosen **and** the CLI path fails the Windows probe [ev:clawteam-spawn-platform#F15][ev:clawteam-spawn-platform#F17].
- No task DAG, mailbox/transport, liveness registry, worktree manager, keepalive or tmux of our own [ev:atm-salvage#F22].
- No Surface adapter: OpenClaw/Telegram stay O4 (MS-02, C-priority) outside the layer; no Gateway dependency [fitgap:MS-01].
- No artifact installer (manifest, lock, report only); no UI; no message bus or router (ATM X1 stays discarded [model:legacy-atm-disposition#2]). ATM code/text may be copied or adapted internally with provenance and preservation of third-party notices [ev:m0-product-architecture-review-2026-08-22#F7].
- Implementation language is not decided here: the layer couples to ClawTeam and the harnesses through CLIs only (assumption (a)).

**Provisional sequencing notes** (not an approved implementation plan)
- Claude Code, Codex, and Grok Build are the required first-pass harness set. Hermes and OpenClaw remain deferred. Exact runs, gates, synthesis, and ordering must be re-baselined in the next PoC plan.
- Operational mode (`watchers[]`, tick evaluator, RunStateSummary, decision log; OS timer default, OpenClaw/Hermes cron pluggable) and the Proposal generator + review CLI — after PoC C.
- The nine bounded ClawTeam PRs of assumption (k) (getuid guard, `mcp<2`, logs + exit code, `created_by` parent link, `TeamMember.hidden`, cleanup stops processes, Hermes/Grok branches + Codex/Grok system prompt, `launch` error surfacing + `AgentDef.profile` + `TaskDef.blocked_by`, inbox membership ACL) — eight of the nine rows of [reuse:§5] (the pre-spawn mutation hook is dropped, see O5) with reuse's guard + pin row counted as two; filed as intent if the owner says so; planned as if none merges.
- Per-adapter `--help` smoke test + verification levels on every HarnessProfile row, re-run on each CLI version bump — the 12-month maintenance contract [panel:judge-2.md#5].
- Interface probes remain preconditions: Codex instruction injection; Claude `--safe-mode --no-session-persistence` under the wrapper; Grok definition/structured-output injection; Hermes hiding only if Hermes enters scope. GitHub-hosted Windows/macOS CI tests deterministic direct-path plumbing at its milestone and ClawTeam subprocess plumbing at its milestone, without live credentials or model-behavior claims [ev:m0-product-architecture-review-2026-08-22#F3][ev:m0-product-architecture-review-2026-08-22#F5].

### 5.1 M-row coverage of the answer

All 35 M-priority rows of the register (`product-intent.md` §3), the §5 component(s) that carry each, the PoC or §3 walkthrough that exercises it, and a status ∈ {full, partial, documented-only, deferred-after-PoC}. "Built, not falsified" marks components inside the M estimate that no PoC criterion can falsify (the AR and EV data objects); the labels are synthesis-authored.

| M row | Carried by (§5) | Exercised by | Status |
|---|---|---|---|
| AD-01 persona, purpose, principles, preferences | Assistant definition package | PoC A (definition under test, hash unchanged) | full |
| AD-02 runtime-neutral Skill/plugin/capability requirements | Assistant definition package; Artifact manifest (capability vs artifact vocabulary) | PoC A (skills dir in the bundle); manifest built, not falsified | full (schema); manifest not falsified by any PoC |
| AD-04 harness policy as data, no permanent binding | HarnessProfiles + HarnessSelectionPolicy resolver | PoC A Runs 1–3 | full |
| AD-05 exclusion list enforceable | Assistant definition package (closed schema + exclusion validator) | PoC A/B/C hash-unchanged checks; hidden-Member audit | partial (structural keys rejected mechanically; free-text project facts detected heuristically) |
| AD-07 Ephemeral Assistants: run-time, hidden, auditable, not forced permanent | Run layer (gate, Ephemeral Assistant writer, roster projections) | PoC B; §3 hidden-Member audit | full in the layer's views (ClawTeam's own board still lists the Member — assumption (f)) |
| AD-08 one definition reusable across templates/runs | Assistant definition package; TeamTemplate by reference | PoC A (three runs) + PoC B (by reference) | full |
| AD-09 Assistant ≠ Skill modelled explicitly | Assistant definition package (a composition of Skills/tools/plugins + harness policy; Skills are referenced Artifacts, never the definition) | PoC A (definition = persona + principles + skills dir + policy, rendered per harness) | full |
| TC-01 compose Assistants by reference | TeamTemplate by reference | PoC B | full |
| TC-02 Lead, relationships, handoff conventions | TeamTemplate by reference (members/Lead/relationships, workflow skeleton); CLI + Skill (coordination section in the bundle) | PoC B (Lead + DAG) | full |
| TC-05 dynamic-member policy | TeamTemplate (policy data); Run layer gate; `CLAWTEAM_BIN` shim | PoC B | partial — gate + convention + shim + post-hoc reconciliation, bypassable by a Lead calling `clawteam spawn` (assumption (d); §3) |
| TC-06 team-level preferences; no project state | TeamTemplate by reference (closed schema, no project fields) | PoC B (template unchanged) | full |
| TE-01 fresh TeamRun with project/task at execution time | Run layer; CoordinationSubstrate (ClawTeam-CLI) | PoC B | full |
| TE-02 fresh instances/sessions; resume opt-in | HarnessBroker/`direct` wrapper (`--safe-mode --no-session-persistence`, `--ephemeral`, isolated homes/profiles); HarnessProfiles | provisional PoC isolation checks | full in design; live behavior unverified |
| TE-03 Members on different harnesses | HarnessAdapter seam; CoordinationSubstrate (per-Member command) | provisional mixed-harness PoC | full in design |
| TE-04 Lead creates a temporary (hidden) Member | Run layer gate + Ephemeral Assistant writer | PoC B | full |
| TE-05 nested TeamRun: result, archive, outer continues | Run layer nesting contract; CoordinationSubstrate | PoC C; §3 nested result + archive | partial — isolation recorded as `namespace`/`data-dir`, not mechanical (assumption (e), §8); carrier pair unverified until PoC C |
| TE-07 auditable archive incl. hidden/nested; project facts stay in the run | Run layer archive writer; HarnessBroker wrapper + ledger | PoC B/C; §3 hidden-Member audit | full |
| TE-08 Ubuntu/Windows/macOS; tmux not the only path | `direct` launcher; ClawTeam subprocess backend with `skip_permissions=false`, no `--task`; platform shims | GitHub-hosted Windows/macOS CI at the corresponding milestones | documented-only until CI; CI proves plumbing, not live auth/model behavior |
| HB-01 HarnessProfile / capability set | HarnessProfiles + policy resolver (seeded from the evidence checklists, verification levels) | PoC A | full |
| HB-02 inject the definition into each harness unchanged | HarnessAdapter seam + adapters | first-pass Claude Code + Codex + Grok Build | partial — interface flags verified; credentialed behavior unverified; Hermes/OpenClaw deferred |
| HB-03 policy precedence per invocation | HarnessSelectionPolicy resolver (deciding layer recorded) | PoC A | full |
| HB-05 Ensemble + synthesis | HarnessBroker + Ensemble record | PoC A Run 3; §3 Ensemble | full (synthesis harness = owner Q9) |
| AR-01 capability vs artifact requirement distinct | Artifact manifest (vocabulary) | none — built, not falsified by any PoC | full (data) |
| AR-02 artifact kinds (Skills, plugins, MCP servers, scripts, binaries, capability packages) | Artifact manifest (`kinds`) | none — built, not falsified by any PoC | full (data) |
| AR-03 sources + metadata to re-establish on another host | Artifact manifest + lock + resolution report (no installer) | none — built, not falsified by any PoC | partial by design — metadata-only in the smallest layer; re-establishment on another host is a manual step |
| AR-04 no secrets in portable configuration | Assistant definition package (validator rejects credential fields); Artifact manifest/lock (references by name only, Hermes `env_requires` pattern) | PoC setup uses env-name credentials only (`minimal-poc-plan.md` §2); built, not falsified by any PoC | full (data rule) |
| EV-01 only project-independent learning persisted | Overlay resolver (overlay schema limits categories); Proposal record `independence_check` | §3 evolution walkthrough | partial now (schema); filter ships with the Proposal generator — deferred-after-PoC C |
| EV-02 overlay model, merge order, conflict rules | Overlay resolver (PoC-A slice) | PoC A (resolver built; no PoC criterion exercises a non-empty overlay) | full (resolver); exercised only if the PoC plan adds an overlay check |
| EV-03 automatic evolution → Proposals, never silent mutation | Overlay resolver + Proposal/review record | §3 evolution walkthrough | deferred-after-PoC C (generator + review CLI); record shape built |
| EV-04 never persist branch/checkpoint/transcripts/project details | Wrapper isolation knobs (`--safe-mode --no-session-persistence`, `--ephemeral`, isolated profile/home); definition validator; archive keeps run facts | provisional leak check | partial (structural/hash checks mechanical; free-text content heuristic) |
| MS-01 Surfaces optional; fully useful locally | CLI + Lead/Member Skill; no Surface adapter | all PoCs (local only) | full |
| MS-04 team semantics never from surface topology | TeamTemplate by reference (no surface fields); no Surface adapter | all PoCs (no Telegram/OpenClaw) | full |
| XC-01 licences/permissions verified before reuse | Vendored modules (MIT, HKUDS attributed); ATM owner authorization + third-party provenance [reuse:§7] | n/a | full for currently identified candidates |
| XC-02 platform limits documented; host vs harness as independent dimensions | HarnessProfiles (platform rows); substrate record (ClawTeam pin, Windows notes) [fitgap:XC-02] | TE-08 probes | full (a documentation requirement; verification is TE-08) |
| XC-03 reuse ladder honoured with evidence | whole composition: rung 1–2 over ClawTeam/harness CLIs, rung 4 vendored, rung 6 only for the data objects of [reuse:§6] | n/a | full |

Reading: 26 full (AD-07 full in the layer's views only) · 7 partial (AD-05, TC-05, TE-05, HB-02, AR-03, EV-01, EV-04) · 1 documented-only (TE-08) · 1 deferred-after-PoC (EV-03); no M row is unsatisfiable by the composition. The AR components (~0.3k) and the Proposal path are inside the M estimate but outside every PoC's falsification path — an owner may add informational checks in `minimal-poc-plan.md`.

## 6. Risks and what would change the answer

| Risk / event | Effect on the answer |
|---|---|
| **ClawTeam upstream revives and merges the PR set** [ev:clawteam-spawn-platform#F24] | two rosters collapse into one; stop-before-cleanup and output capture shrink; TC-05/TE-05 can become mechanical; O5 rises, the layer shrinks ≈0.5–1k; seams and data objects stay |
| **Hermes kanban gains nested boards + foreign-harness workers** [ev:claude-agent-teams-hermes-openbot#F25] | Hermes becomes a credible second CoordinationSubstrate, cheaper for all-Hermes operational teams; a second seam implementation is then justified (dissent 5) |
| **Claude Code teams leave experimental and accept non-Claude members / headless spawn** [ev:claude-agent-teams-hermes-openbot#F2][ev:claude-agent-teams-hermes-openbot#F6][ev:claude-agent-teams-hermes-openbot#F8] | a second substrate for Claude-led teams; same consequence |
| **Windows probe fails on the CLI path** (cmd.exe, `shell=True`, no exit code even with the wrapper) [ev:clawteam-spawn-platform#F10][ev:clawteam-spawn-platform#F20] | adopt C's in-process `SpawnBackend` (Python) with registry entries written as `backend: subprocess` + pid, or O6 vendoring of `store/file.py` + `transport/file.py`; the seam keeps the rest unchanged |
| **Codex `-c developer_instructions` not honoured** [ev:harness-cli-capabilities-a#F2] | PoC A Run 1 starts degraded on the `AGENTS.md`-in-`CODEX_HOME` channel (`degraded[]`); answer unchanged |
| **Per-run `CLAWTEAM_DATA_DIR` breaks the creating Member's `task wait`/inbox discovery** [model:team-execution-model#13] | PoC C records `independence.achieved: namespace`; mechanical isolation waits for the ACL PR or spawn-path ownership |
| **ClawTeam bit-rot** (`mcp` 2.0 already broke `clawteam-mcp` [ev:clawteam-probe-log#F1]) | pin `0.3.0@0119833`, `mcp<2`, uv Python 3.11; record the pin in every TeamRun's substrate record; replacement = one CoordinationSubstrate implementation |
| **Harness CLI flag drift** | same five-adapter burden in every option; profiles-as-data with verification levels + `--help` smoke test |
| **Lead tool-call non-compliance** (ATM U2 0/2 [ev:atm-salvage#F24]) | protocol state in the DAG and in `ats` CLI calls with structured args, never prose; deterministic no-op-harness tier per PoC (owner question) |
| **Scope creep** (ATM R5 [model:legacy-atm-disposition#4]); **native-auth policy drift** [ev:harness-cli-capabilities-a#F15] | re-baseline the first-pass slice before implementation; owner-operated subscription OAuth only; separate API-test profiles; re-check policy before any multi-user/hosted product |

## 7. Review dispositions and remaining owner questions

Resolved in the current review: Windows/macOS use credential-free GitHub-hosted CI at the relevant milestones; advisory/bypass-visible/audited enforcement is acceptable in PoCs while production requires mechanical enforcement; owner-operated subscription OAuth is required for native/unattended live runs; ATM internal reuse is authorized; and Claude Code + Codex + Grok Build form the first-pass harness set.

Still open: implementation language; whether/when to file the ClawTeam PRs; whether Hermes belongs in a later mixed-team expansion; live-run budget and deterministic tier; synthesis design/cost derivation; Member coordination protocol; final project name; and the detailed PoC run/schema/sequence choices. `.project-steward/QUESTIONS.md` is the current authoritative list.

## 8. Inconsistencies noted with other documents

- **Broker location and scheduler substrate are decided here** (left open by [model:harness-broker-model#6] / §13 Q7 and [model:team-execution-model#13] Q3): the HarnessBroker runs inside the layer as library/CLI (options (a) + (c)), never as a ClawTeam backend (option (b) is the Windows fallback only); the operational-mode scheduler is an OS timer by default with OpenClaw/Hermes cron pluggable. No contradiction; both model documents now point here (`harness-broker-model.md` §6/§13 Q7, `team-execution-model.md` §13 Q3).
- **Isolation-level vocabulary.** The run record carries `independence: {declared: advisory|mechanical, achieved: namespace|data-dir|mechanical}`. `team-execution-model.md` §2 declares TC-03 independence `level: advisory | mechanical` (the *declared* field) and §6 item 4 states the isolation rule but records no *achieved* value — `independence.achieved` does not occur there. The achieved vocabulary originates in the panel judges ([panel:judge-1.md#5] item 5: `namespace | data-dir`; [panel:judge-2.md#5] item 5: `advisory|mechanical`) and was fixed by the owner tiebreak assumption (e) (§5.0) as `namespace | data-dir | mechanical`; on ClawTeam today achieved = `namespace` (no inter-team boundary [ev:clawteam-probe-log#F19]) or `data-dir` if a per-run `CLAWTEAM_DATA_DIR` is used (bypassable by the global `--data-dir` flag [ev:clawteam-probe-log#F2]); `mechanical` requires the inbox-ACL PR or owning the spawn path. *(resolved in the W3 fix pass: `team-execution-model.md` §2/§3.3/§6 item 4 now carry `independence {declared, achieved}` with this provenance; `minimal-poc-plan.md` §5 uses it.)*
- **Solo runs outside ClawTeam.** `product-intent.md` §4 PoC A does not require a team; this document runs PoC A on the `direct` launcher (Judge 1 Q6, resolved by the tiebreak). `minimal-poc-plan.md` must not require a one-Member ClawTeam team for PoC A.
- **Size figures.** Panel estimates remain historical. The current answer retains M (≈4–6k including profile data) only as a discovery estimate; the first-pass slice and its LOC must be re-baselined after the detailed PoC plan is reviewed.
- **Nested-run result carrier.** `clawteam task update --metadata` does not exist as a CLI verb in ClawTeam 0.3.0@0119833 (`ClawTeam/clawteam/cli/commands.py:2181` — only `--status/--owner/--subject/--description/--priority/--add-blocks/--add-blocked-by/--force`); the carrier fixed here (§3, §5 table) is RunResult in the outer `run.json` + inner archive, `inbox send` as the signal, `task update --status completed` to close the delegated task. *(resolved in the W3 fix pass: `team-execution-model.md` §6/§8/§13 Q1, `minimal-poc-plan.md` §1/§5/§7 and the `existing-systems-fit-gap.md` evidence-gap lines now state the same carrier; the probe log carries an owner note.)*
- **TC-05 enforcement level.** The achievable level on ClawTeam today is stated once in §3 PoC B (owner assumption (d) + the bypass fact [ev:clawteam-spawn-platform#F16]); *(resolved: `team-execution-model.md` §5 carries it.)*
- No fit-gap cell, reuse rung or register ID is contradicted; the panel's "two rosters", "TC-05 bypass" and "namespace-only isolation" caveats match [fitgap:TC-04], [fitgap:TC-05] and [fitgap:TE-05] as written on 2026-08-22 (the fit-gap fix pass re-scored TE-05 and TE-07 CT to `Xs!` for the nesting/archive glue; this document's "new objects over ClawTeam mechanics" wording agrees).

STOP — this answer (§5) is for accept/reject review by the product/architecture reviewer; nothing in §3, §5 "Deferred" or §6 is an implementation task until that review decides (`product-intent.md` §6).
