---
critic: critic:architecture-options
document: docs/discovery/architecture-options.md (status draft, date 2026-08-22, 173 lines, 6,154 words)
date: 2026-08-22
---

## 1 Verdict

**FAIL** (one BLOCKER; fixable in one pass). The document's option comparison, walkthroughs and answer are evidence-disciplined to an unusually high degree — every load-bearing ClawTeam mechanism it relies on (`os.getuid()` only inside `if skip_permissions:`, `lifecycle on-exit` appended regardless of `--no-keepalive`, `is_agent_alive`/`stop_agent` handling only `tmux|subprocess|wsh`, prompt built only when `--task` is given, generic `-p` append only `elif prompt:`, HEAD `0119833` of 2026-05-09) re-verifies against `/home/wsh/Documents/00000/ClawTeam` source, and the answer paragraph is byte-identical in `README.md` and `minimal-poc-plan.md`. It fails on verifiability of its own authority: nineteen places adopt "assumptions (a)–(l)" that the document says the owner tiebreak "fixed", "listed in §4" and "quoted in full in §5" — but no list (a)–(l) exists anywhere in the document, in `evidence/panel/owner-tiebreak.md` (the verbatim note, which has no lettered assumptions), or in any panel file (grep "assumption" → 0 hits). Since those assumptions carry the walkthrough mechanics (b, c, g), the isolation vocabulary (e), the PoC preconditions (j), the PR policy (k) and the cost rule (l), a reviewer cannot tell what the owner actually decided versus what the synthesis decided in the owner's name. Two MAJORs compound it: §8 attributes `independence.achieved` to `team-execution-model.md` (0 hits there) and to "assumption (e)", and the §5 table repeats verbatim a claim ("bypasses ClawTeam's adapter chain by explicit command") that §4 of the same document accepts as only half-true. Fix those three, and the document is PASS-WITH-MINORS.

## 2 Findings table

| id | severity | check | location | problem | evidence | suggested fix |
|---|---|---|---|---|---|---|
| AO-01 | **BLOCKER** | C11, C12, C2 | §1 last sentence ("its assumptions (a)–(l) adopted verbatim in §3 and §5"); §5 blockquote ("fixed assumptions (a)–(l) listed in §4; the full note is preserved at `evidence/panel/owner-tiebreak.md`"); 19 references: §3 (b)(c)(d)(e)(f)(g)(i)(j)(l), §4 (b)(c), dissent 5 (h), §5 (a)(j)(k), §8 (e)(i) | Assumptions (a)–(l) are never enumerated; "listed in §4" is false (§4 lists none); "quoted in full in §5" is false (§5 quotes one sentence); the preserved note contains no lettered assumptions, so the owner is credited with constraints the owner's note does not contain | `grep -c "assumption" evidence/panel/*.md` → 0; `evidence/panel/owner-tiebreak.md` (1,902 bytes, verbatim) has no "(a)"…"(l)"; the arch doc's only (a)–(l) text is the 19 passing references (`grep -o "assumption[s]* ([a-l])"`) | Add an "Assumptions (a)–(l)" list (verbatim, one line each) in §1 or §5, say who authored each (owner note vs synthesis), and correct "listed in §4"/"quoted in full in §5"; if the owner did state them outside the preserved note, append them to `owner-tiebreak.md` |
| AO-02 | MAJOR | C11, C2 | §8 bullet 2 "Isolation-level vocabulary": "`team-execution-model.md` §2/§6 declares TC-03 independence as `advisory | mechanical` and records `independence.achieved` for nested runs; the tiebreak (assumption (e)) fixes the *achieved* value set as `namespace | data-dir | mechanical`" | `independence.achieved` does not occur in `team-execution-model.md`; it originates in judge amendments (judge-1 §5 #5 `namespace | data-dir`; judge-2 §5 #5 `advisory|mechanical`), and the tiebreak note contains no (e); the three-valued set is the synthesis' own merge presented as an owner decision | `grep -n "achieved" team-execution-model.md` → no output; judge-1.md §5 item 5; judge-2.md §5 item 5; `owner-tiebreak.md` | Re-state: "`independence.achieved` is introduced here (from [panel:judge-1.md#5] and [panel:judge-2.md#5]); `team-execution-model.md` §6 item 4 should adopt it" — and drop the tiebreak attribution unless AO-01's list shows the owner wrote it |
| AO-03 | MAJOR | C12, C7 | §5 table row "HarnessAdapter seam + 5 adapters", column "What it reuses": "bypasses ClawTeam's adapter chain by explicit command" (copied verbatim into `minimal-poc-plan.md` §1 table) | Contradicts §4 row 1, where the document *accepts* the judge finding that this claim is "only the profile half right: `prepare_command` still runs; an unrecognised basename falls into the generic branch and gets `-p <prompt>` appended when a prompt exists" | `ClawTeam/clawteam/spawn/adapters.py:45-60,128-140` (prepare_command runs for every command; `elif prompt:` appends `-p`); [ev:clawteam-spawn-platform#F5] (explicit command suppresses only the implicit profile) | Replace the cell with "explicit positional command suppresses ClawTeam's implicit profile [F5]; `prepare_command` still runs but appends nothing when no `--task` is given (assumption (b))" and re-copy into poc-plan §1 |
| AO-04 | MAJOR | C12 | §5 answer paragraph: "two seams … implemented once each today: five harness adapters (claude-code, codex, grok, hermes, openclaw) … and one ClawTeam adapter" vs. §5 "Deferred": "grok/hermes/openclaw adapters after the two PoC-A adapters" (paragraph is copied into README.md and minimal-poc-plan.md) | Two readings: (i) "today" = five adapters exist/are in scope now; (ii) five are planned, two are built first. "Implemented once each" is also self-contradictory for a seam said to have five implementations | §5 paragraph vs §5 Deferred bullet 1; README.md "Answer" paragraph identical (verified by diff) | Reword: "HarnessAdapter has one adapter per installed harness (five planned; claude-code and codex in the PoC-A slice, the other three deferred); CoordinationSubstrate has exactly one implementation (ClawTeam CLI) plus the trivial `direct` launcher" and re-copy |
| AO-05 | MINOR | C2 | §2 O4: "or Hermes `send --to` [ev:claude-agent-teams-hermes-openbot#F26]" | F26 documents `hermes peer dm`, gateway platforms and profile routing; no evidence file contains `hermes send` or `send --to` (the phrase traces only to fit-gap MS-02 and reuse G-MS-02, both citing F26) | `grep -rn -i "send --to\|hermes send" evidence/*.md` → 0 hits; F26 text | Cite the actual CLI help (`hermes send --help`, with access date) or drop the command name and say "Hermes gateway platforms [F26]" |
| AO-06 | MINOR | C2 | §2 O4: "OpenClaw channels (`agent --deliver`, bindings, `identity`) [ev:openclaw-native-and-telegram-verification#F20]" | F20 is the "agentId routes the brain, not the sender" finding; `--deliver` appears only in that file's §6 CLI log (line 327) and in [ev:clawteam-openclaw-fork-delta#F5] | `grep -n -F "deliver" openclaw-native-and-telegram-verification.md` → lines 66, 70, 194, 261, 327 only; fork-delta F5 lists `openclaw agent --help … --deliver` | Cite [ev:clawteam-openclaw-fork-delta#F5] or the openclaw §6 log for `--deliver`; keep F20 for `identity`/routing |
| AO-07 | MINOR | C2 | §2 O7: "gives ACP children prompt + cwd only [ev:openclaw-native-and-telegram-verification#F16][ev:harness-cli-capabilities-b#F12]" | Neither finding says what an ACP child receives; F16 shows `acp{agent, backend, cwd, mode}` config keys, F12 describes native sub-agents and the acpx alias list; "AGENTS.md + TOOLS.md only" (openclaw F14) is about *native* sub-agents | F12 and F16 full text (read); openclaw F14 | Say "ACP config carries agent/backend/cwd/mode only; persona delivery to an ACP child is unevidenced (`?`)" |
| AO-08 | MINOR | C2, C11 | §2 O2 "*Ladder:* … rung 6 only for the 23 data objects [reuse:G-XC-03]" | G-XC-03 speaks of "three genuinely new objects"; "23" is the rung-6 *gap count* in reuse §2 "Reading across", which says "most" (not all) rung-6 items are data objects | reuse-vs-build-analysis.md:117 ("rung 6: 23 … Most rung-6 items are data objects"); G-XC-03 row line 46 | Cite reuse §2 "Reading across" and write "23 rung-6 gaps, mostly data objects" |
| AO-09 | MINOR | C11 | §2 O5 "the nine bounded PRs of [reuse:§5] (enumerated under 'Deferred' in §5 …)" vs §5 Deferred bullet 3 | reuse §5 has nine rows *including* the pre-spawn mutation hook and *merging* getuid-guard + mcp-pin into one row; the Deferred list drops the hook and splits guard/pin — both say "nine" but are different sets | reuse-vs-build-analysis.md:149-163 (nine rows); arch §5 Deferred bullet 3 | Write "eight of reuse §5's nine PRs (the mutation hook dropped, see C §8), with the getuid guard and `mcp<2` pin counted separately" |
| AO-10 | MINOR | C12 | §8 bullet 4: "`README.md`'s 'Answer' line is to be filled from §5" | Stale: README.md already carries the §5 paragraph verbatim (diff = identical) | python diff of the `**The smallest new software layer is**` paragraph: README == ARCH True | Replace with "README.md carries the paragraph verbatim and the table by reference" |
| AO-11 | MINOR | C7 (cross-doc) | README.md "Answer" (paragraph only) | Brief requires paragraph **+ table** identical in README; README omits the table ("are in architecture-options.md §5") | README.md lines 9-13; table regex absent in README | README owner: paste the §5 table (or keep by-reference and have the brief accept it); arch doc §8 should note which was chosen |
| AO-12 | MINOR | C5 | §3 "Nested TeamRun result + archive": "(record; unverified with real agents, probe open question 3)"; §4 "Coverage gaps": "nested result carrier unverified with real agents" | "agents" used for AssistantInstances/Members, not as ClawTeam's or another system's term | glossary.md Assistant row ("Do NOT call it agent"); text at lines 64, 93 | "unverified with real Members / live harness invocations" |
| AO-13 | MINOR | C6 | §3 PoC C: "inner Members get … worktrees based on t4's branch [ev:clawteam-probe-log#F22]" | F22 shows `--repo` creates a worktree (`base_branch` recorded) off the repo's current branch; no mechanism shown for choosing the base branch; `team-execution-model.md` §6 step 11 marks this "inferred" | probe-log F22 text; team-execution-model §6 walkthrough item 11 | State the mechanism (check out t4's branch in the repo handed to `--repo`, or create worktrees outside ClawTeam with `--no-workspace`) or mark it `?` for PoC C |
| AO-14 | MINOR | C10 | whole document | Harness CLI versions are never pinned here (Claude Code 2.1.239, Codex 0.148.0, Grok 1.0.5, OpenClaw 2026.7.1-2, Hermes 0.20.4 appear nowhere); only ClawTeam `0.3.0@0119833` is pinned | `grep -c "2.1.239\|0.148.0\|1.0.5\|2026.7.1\|0.20.4" architecture-options.md` → 0 | Add one "installed versions" line in §1 (or §5 next to the ClawTeam pin) and refer to it from §3 |
| AO-15 | MINOR | C12 | frontmatter `depends_on`: "panel outputs scratchpad/arch/{…}.md" | Points at a non-durable /tmp scratchpad path; identical copies live in the repo at `evidence/panel/` (diff -q: all five identical) | `ls evidence/panel/`; diff against scratchpad/arch | Point `depends_on` and `[panel:…]` anchors at `evidence/panel/` |
| AO-16 | MINOR | C2 | §2 O6 "Reading 2 (B, C, adopted here): a sourcing rule, not a layer" | C's O6 shape explicitly vendors ClawTeam `store/file.py` + `transport/file.py` (task DAG + inboxes) — the substance of Reading 1 ("replace ClawTeam's task/inbox"); only B frames O6 as pure sourcing | proposal-C §2 O6 shape; proposal-B §2 O6 ("O6 is a sourcing strategy, not a layer") | Attribute Reading 2 to B, note C straddles both readings (store/transport vendoring is its fallback) |
| AO-17 | MINOR | C12, C1 (item 19) | end of document (§8) | No explicit STOP; §5 "Deferred until after PoCs" + "run layer gated on PoC A passing" read as an implementation sequence | product-intent §6 (project STOPs for review); minimal-poc-plan ends with an explicit STOP line | Add a closing line: "STOP — this answer is for accept/reject review; nothing in §5 is an implementation task until the review decides" |
| AO-18 | MINOR | C12 | §1 "Consolidated option scores in §2 are the range across the three architects' option tables" + §2 table "Total" column | Totals are ranges of the three proposals' own totals (O2 35–42), not sums of the column ranges (O2 column mins sum to 34, maxes to 43) — a reader adding columns gets a mismatch | §2 table O2 row; proposal-B §2 O2 = 36, proposal-A O2 ≈ 42, judge-1 §3 (C's O2 = 35) | Add "Total = range of each proposal's own total" under the table |
| AO-19 | MINOR | C2 | §1 "Both independently endorsed the same named composition — 'A's O2 scope with B's two seams declared; C's PR set filed as good citizenship; C's in-process `SpawnBackend` kept only as the documented Windows fallback' [panel:judge-1.md#5][panel:judge-2.md#5]" | The quoted wording is judge-2's; judge-1 §5 endorsed "B's ownership model and two seams, built at A's scope, with C's caveats and C's PRs as optional accelerators" and mentions the in-process route only in its dissent | judge-1.md §5 first paragraph; judge-2.md §5 first paragraph | Quote each judge's own sentence, then state the overlap |
| AO-20 | NIT | C2 | §2 O5 "#82/#88 open since 2026-03-24 … [ev:clawteam-spawn-platform#F24]" | F24 does not mention #82/#88 (judge-2 §3 notes they were "uncited in the evidence corpus"); the fact rests on the judges' `gh` re-check | F24 text; judge-2.md:66 | Cite [panel:judge-1.md#1][panel:judge-2.md#3] for #82/#88 and F24 for the cadence only |
| AO-21 | NIT | C4 | §3 PoC B "post-hoc reconciliation of `spawn_registry.json` against the roster"; §7 Q3 | "reconciliation" is a demoted ATM noun (desired-state reconciliation); what is meant is a post-hoc audit diff | glossary "ATM terms that are demoted"; legacy-atm-disposition D4 | Say "post-hoc audit/diff of `spawn_registry.json` against the roster" |
| AO-22 | NIT | C11 | §2 O1 "*Cannot satisfy (M):* … AR-01/02/03 [fitgap:AD-01][fitgap:TC-01][fitgap:HB-01][fitgap:EV-03]" | No AR or TE cell is cited for the AR/TE items in the list | fit-gap AR layer rows 718-813 | Add [fitgap:AR-01][fitgap:TE-05][fitgap:TE-07] |
| AO-23 | NIT | C12 | whole document | 6,154 words against the 3,500–6,000 target | `wc -w` | Trim §3 repetition (PoC C vs "Nested TeamRun result" overlap) |

## 3 Citation spot-check log

Verified against the evidence files, panel records, model documents and the read-only ClawTeam checkout (`/home/wsh/Documents/00000/ClawTeam`, HEAD `0119833` 2026-05-09).

| Citation (as used) | Claim in the document | Result |
|---|---|---|
| [ev:clawteam-spawn-platform#F24] | upstream HEAD 0119833 / 2026-05-09; 22 open PRs; #159/#165/#167 | **OK** (`git log -1` confirms HEAD + date); #82/#88 **not in F24** (AO-20) |
| [ev:clawteam-probe-log#F1] | `clawteam-mcp` broken by unpinned `mcp` (2.0.0); `mcp<2` fixes | OK |
| [ev:clawteam-spawn-platform#F20] | `os.getuid()` under default `skip_permissions`; no keepalive/exit code on Windows; cmd.exe truncation; no Windows CI | OK |
| [panel:judge-1.md#1] | `os.getuid()` sits inside `if skip_permissions:` | **OK in source** (`adapters.py:49-53`) |
| [panel:judge-1.md#1] + [ev:clawteam-probe-log#F12] | generic branch appends `-p` only `elif prompt:`; prompt built only when `task` given | **OK in source** (`adapters.py:131-140`, `commands.py:3251`) |
| [panel:judge-1.md#1] + [ev:clawteam-probe-log#F14] | `--no-keepalive` does not suppress `lifecycle on-exit` | **OK in source** (`keepalive.py:64-69`: `if not keepalive or not resume_command: return f"{cmd_str}; __ct_status=$?; {exit_hook}; exit $__ct_status"`) |
| [panel:judge-1.md#1] + [ev:clawteam-probe-log#F8] | custom backend name invisible to `is_agent_alive`/`stop_agent`; locks never releasable | **OK in source** (`registry.py:55-79,121-168` handle only tmux/subprocess/wsh) |
| [ev:clawteam-spawn-platform#F5] | explicit positional command suppresses implicit profile | OK (profile half only — see AO-03) |
| [ev:clawteam-spawn-platform#F15][#F16][#F17] | Alpha seams, no `__all__`; `BeforeWorkerSpawn` does not fire on CLI paths / cannot mutate; plugins only under `harness conduct` | OK |
| [ev:clawteam-spawn-platform#F21] + [ev:clawteam-probe-log#F14] | DEVNULL; `exit_code: null` | OK |
| [ev:clawteam-spawn-platform#F6] | system prompt dropped for Codex (`--skill` → claude/pi only) | OK |
| [ev:clawteam-spawn-platform#F8] | `SpawnBackend.spawn` receives prompt/system_prompt/command | OK |
| [ev:clawteam-probe-log#F2][#F3] | global `--data-dir` on every command; `CLAWTEAM_DATA_DIR` env | OK |
| [ev:clawteam-probe-log#F7][#F9][#F13] | DAG (dangling `--blocked-by` accepted), messaging, registry `S!` | OK |
| [ev:clawteam-probe-log#F15][#F23] + [ev:clawteam-spawn-platform#F12] | `launch` swallows errors, one profile for all, no DAG edges | OK |
| [ev:clawteam-probe-log#F16][#F17] | per-Member command by TOML; precedence CLI > profile > claude (inverted) | OK |
| [ev:clawteam-probe-log#F18][#F19][#F20][#F22] | child becomes leader; no isolation boundary; cleanup leaves processes; `--repo` worktree | OK (F22 does not show base-branch choice — AO-13) |
| [ev:clawteam-model#F5][#F8][#F13][#F15][#F16][#F17][#F18][#F22][#F28] | first spawn = leader; `task wait` drains leader inbox; `AgentDef.task`; worker loop; Skill; plan round-trip; LeaderWatcher; nested walkthrough; self-reported CostEvent | OK |
| [ev:clawteam-openclaw-fork-delta#F5][#F15][#F19][#F28][#F30] | OpenClaw flag drift; `model_resolution.py` 70 lines; `platform_compat.py` 130 lines stdlib; standalone; MIT no SPDX | OK |
| [ev:openclaw-native-and-telegram-verification#F9][#F16][#F21] | cron `--command`; acpx absent, Gateway needed, 18 aliases; maxSpawnDepth/archive | OK |
| [ev:openclaw-native-and-telegram-verification#F20] | `agent --deliver` | **wrong anchor** (AO-06) |
| [ev:openclaw-native-and-telegram-verification#F16] + [ev:harness-cli-capabilities-b#F12] | "ACP children prompt + cwd only" | **not supported** (AO-07) |
| [ev:harness-cli-capabilities-a#F2][#F10][#F14][#F15][#F16][#F17][#F19] | Codex `-c developer_instructions` observed-only; `--append-system-prompt-file` in binary not `--help`; cost fields; platforms; ToS; checklist; injection matrix; `--bare` | OK |
| [ev:harness-cli-capabilities-b#F6][#F11][#F16][#F19][#F23][#F24] | Hermes `chat --source tool -q`; OpenClaw skills eligibility; WSL2; profile clone; checklist; injection matrix | OK |
| [ev:claude-agent-teams-hermes-openbot#F2][#F6][#F7][#F8][#F24][#F25] | teams experimental / Claude-only / non-nested / interactive-only; `distribution.yaml`; kanban no nested boards | OK |
| [ev:claude-agent-teams-hermes-openbot#F26] | Hermes `send --to` | **not supported** (AO-05) |
| [ev:dsh-agent-teams-and-gui#F20]; [ev:atm-salvage#F10][#F22][#F24] | DG `.strict()`; ADR 0022; "do not build a mailbox runtime"; U2 0/2 | OK |
| [fitgap:TE-01] (TE layer summary) | "not genuinely new — reachable at reuse rung 1–3" | OK (text is in the TE roll-up, not the TE-01 row — anchor loose) |
| [fitgap:TE-03][fitgap:TE-05][fitgap:TE-08][fitgap:TC-04][fitgap:TC-05][fitgap:HB-02][fitgap:HB-05][fitgap:HB-07][fitgap:MS-01][fitgap:MS-04][fitgap:XC-03] | cells and caveats as quoted | OK (TE-05 OC P!/HM P! consistent with "none hosts mixed-harness nested today" because TE-03 OC C! is acpx-contingent, HM M!) |
| [reuse:G-AD-01][reuse:G-XC-03][reuse:§5][reuse:§7] | rung 6 objects; "rung 3 never the only path"; nine PRs; ATM unlicensed | OK except "23 data objects" (AO-08) and PR-set drift (AO-09) |
| [model:harness-broker-model#6/#7/#8/#9/#10/#13]; [model:team-execution-model#2/#3/#5/#6/#7/#13]; [model:assistant-domain-model#8/#9]; [model:legacy-atm-disposition#2/#4] | options a–e; record; ensemble; deterministic backends; adapter seam; Q5–Q7; template; lifecycle; hidden member; nesting contract; operational; Q3/Q5; ephemeral/promotion; overlays; X1; R5/R11 | OK — **except** "`independence.achieved`" claimed for team-execution-model (AO-02) |
| [panel:judge-1.md#2/#3/#5/#6/#7]; [panel:judge-2.md#2/#3/#5/#6/#7]; proposals A/B/C §2, §4, §8 | scores 40/41/31 and 39/37/33; sizing note; factual-error table; dissent; owner questions; option tables; Q9 | OK (composition quote is judge-2's wording — AO-19) |
| `evidence/panel/owner-tiebreak.md` | "assumptions (a)–(l)", "quoted in full in §5" | **FAILED** — note contains no assumptions; §5 quotes one line (AO-01) |
| Cross-doc identity | §5 paragraph/table vs README.md and minimal-poc-plan.md | paragraph identical in both; table identical in poc-plan; README has no table (AO-11) |

Totals: 78 citations/anchors checked; 6 failed (3 unsupported: `send --to`, "ACP prompt + cwd only", `independence.achieved` in team-execution-model; 3 wrong/loose anchors: `--deliver`→F20, "23 data objects"→G-XC-03, #82/#88→F24) plus the BLOCKER cross-reference to a non-existent assumptions list.

## 4 Coverage map (brief items relevant to this document)

| # | Brief item | Status | Where / gap |
|---|---|---|---|
| 1 | portable Assistant definition (contents, exclusion list, persistent vs fresh) | covered | §5 row 1 (closed schema + exclusion validator); walkthroughs assert hash-unchanged |
| 2 | Assistant ≠ Skill | partial | never named (AD-09 not cited); implied by the definition package row only |
| 3 | Assistant ≠ harness (Profile/Capability/Policy/Broker/Invocation/Ensemble) | covered | §3 PoC A + Ensemble; §5 rows 4–5; broker location decided (§8) |
| 4 | TeamTemplate contents, no project state | covered | §5 row 2 |
| 5 | TeamRun fresh instances/context, run-time workspace, temp state | covered | §3 PoC B; §5 Run layer |
| 6 | persistent + temporary hidden auditable members | covered | §3 PoC B + hidden-member audit; two-roster honesty |
| 7 | nested dynamic teams (pressure test) | covered | §3 PoC C + result/archive; nesting contract row |
| 8 | long-running operational Assistants | covered | §3 operational walkthrough; scheduler substrate decided; deferred post-PoC C |
| 9 | reviewed evolution (Base + User + Reviewed overlays; Proposals) | covered | §3 evolution walkthrough; §5 row 3 |
| 10 | artifact portability (semantic vs explicit; vendored + git; no secrets) | partial | §5 row 6 covers manifest/lock/report; "no secrets" (AR-04) not mentioned anywhere |
| 11 | messaging optional; visible identity = presentation | covered | O4; "No Surface adapter"; MS-01/MS-04 satisfied without it |
| 12 | study ClawTeam as-is/configured/extended/wrapped; fork; kit | covered | O1/O2/O3/O5 (as-is/wrapped/extended), O6 (fork vendoring), O4 (kit/OpenClaw) |
| 13 | other systems as behavioral references (which layer each solves) | covered | O7 evidence on CC teams / OpenClaw / Hermes; Hermes positioned as Harness (dissent 5) |
| 14 | reuse ladder + licence verification | covered | R6/R9 per option; licence per option; [reuse:§7] cited; MIT/HKUDS attribution |
| 15 | no ATM sunk cost (demotion list) | covered | ATM X1 discarded; R5/R11/U2 used as risks; no demoted noun re-enters (one wording nit AO-21) |
| 16 | fit-gap classification by 8 layers | covered (by reference) | cells quoted faithfully; §8 states no cell contradicted |
| 17 | minimal PoCs A/B/C, no Telegram/OpenClaw | covered | §3 walkthroughs; day-one probes; "PoC A on `direct`" |
| 18 | cross-platform Ubuntu/Windows/macOS; tmux not the only path | covered | per-option tmux-free/Windows line; §6 Windows risk; Q2 |
| 19 | nine deliverables + single "smallest layer" answer comparing 8 options; then STOP | partial | answer + 8-option comparison present; **no explicit STOP** (AO-17); authority of the answer unverifiable (AO-01) |

## 5 What must change before the owner's review

1. **Enumerate assumptions (a)–(l)** verbatim, attribute each (owner note vs synthesis), and correct "listed in §4" / "quoted in full in §5" (AO-01). Until this exists, §3, §5 and §8 invoke an owner decision that the preserved note does not contain.
2. **Fix the `independence.achieved` provenance** in §8: it is new here (from the two judges' amendments), not in `team-execution-model.md` and not in the tiebreak (AO-02).
3. **Remove the self-contradicted cell** "bypasses ClawTeam's adapter chain by explicit command" from the §5 table and re-copy the table into `minimal-poc-plan.md` (AO-03); the same document's §4 row 1 says `prepare_command` still runs.
4. **Disambiguate "implemented once each today: five harness adapters"** in the answer paragraph and re-copy it into README.md and minimal-poc-plan.md (AO-04).
5. **Repair the three unsupported/wrong anchors** (`send --to`→F26, `--deliver`→F20, "ACP children prompt + cwd only"→F16/F12) and the two loose ones ("23 data objects"→G-XC-03, #82/#88→F24) (AO-05…AO-08, AO-20).
6. **Reconcile the PR set** ("nine of reuse §5" ≠ the nine enumerated in §5 Deferred) (AO-09); update the stale README remark (AO-10); pin harness versions once (AO-14); point `depends_on` at `evidence/panel/` (AO-15); add the STOP line (AO-17).
7. Glossary/wording: "real agents" → Members/invocations (AO-12); "reconciliation" → audit/diff (AO-21); attribute Reading 2 of O6 to B, not C (AO-16); quote the judges' endorsements separately (AO-19); add the Total-column note (AO-18); state the base-branch mechanism for inner worktrees or mark it `?` (AO-13).


---

## Closure note (2026-08-23, G1 documentation hygiene — review H8)

This file is a dated W3 critic record (2026-08-22) and is not re-verified here. The verdict above is the last recorded critic verdict for this document: the fix pass that followed applied decisions D1–D15 (`owner-decisions-fix-pass.md` — session-authored in the owner's voice and reclassified as historical fix-pass inputs by ADR 0018), and the PLAN/PROGRESS claim "re-checks all PASS" has no re-check artefact in the tree, so no re-check PASS is asserted for this document on that basis. Spot-checked at G1: the AO-01 BLOCKER (references to tiebreak assumptions (a)–(l) that no panel file contained) no longer reproduces — `evidence/panel/owner-tiebreak.md` now carries the assumptions inline (its frontmatter notes the earlier truncated copy), and ADR 0018 records that those assumptions are session-authored panel inputs rather than owner decisions. What closes these findings today: the reviewed document was re-baselined on 2026-08-23 (Python/`uv` core, optional ClawTeam provider — ADR 0014/0015); the approved M1a plan (`docs/plans/m1a-direct-harness-poc.md`, ADR 0021) supersedes the M0 PoC design this critique targets; and the independent review of `3407ec9` (`docs/reviews/2026-08-23-m0-review-at-3407ec9.md`, ADR 0018) re-audited the whole package, with its open items tracked in the M1a plan, `QUESTIONS.md`, and ADR 0022. The findings below the verdict are historical evidence, not an open work list.
