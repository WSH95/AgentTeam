# Owner decisions for the W3 fix pass (binding for every fixer; 2026-08-22)

These settle the cross-document findings so that all documents say ONE thing. Apply them verbatim where your document is named; do not re-decide them.

D1. **Nested-run result carrier.** `clawteam task update --metadata` does NOT exist as a CLI verb in ClawTeam 0.3.0@0119833 (only --status/--owner/--subject/--description/--priority/--add-blocks/--add-blocked-by/--force). The binding carrier is: the layer writes the RunResult into the OUTER run's `run.json` (layer-owned) and into the inner run's archive; it signals the creating Member with `clawteam inbox send <outer-team> <creating-member> <RunResult summary>` (probe-verified cross-team, [ev:clawteam-probe-log#F19]); the delegated outer task is closed with `clawteam task update <outer-team> <task-id> --status completed` (optionally `--description` carrying a one-line pointer to the RunResult path). No metadata verb anywhere. Apply identically in team-execution-model §6/§8 (and §13 Q1), architecture-options §3 "Nested TeamRun result + archive" + §5 table row "Run layer", minimal-poc-plan §1/§5/§7, existing-systems-fit-gap evidence-gap lines (replace "inbox send vs task update --metadata" with "inbox send + run.json; task close by --status").

D2. **PoC C criterion 1.** Restate as: "no inner DAG task and no inner *content* message appears in the outer team's tasks/inboxes/events; ClawTeam lifecycle notices (on-exit/idle/join/shutdown message types) addressed to the inner team's leader are excluded by type, and the check is evaluated on the archive's filtered projection recorded by the layer". Keep criterion 2 consistent with D1.

D3. **Isolation vocabulary (team-execution-model §2/§6 item 4/§3.3 sketch; architecture-options §8; minimal-poc-plan §5).** `independence: {declared: advisory|mechanical, achieved: namespace|data-dir|mechanical}`; on ClawTeam today achieved = `namespace` (no inter-team boundary, [ev:clawteam-probe-log#F19]) or `data-dir` if a per-run `CLAWTEAM_DATA_DIR` is used; `mechanical` requires the inbox-ACL PR or owning the spawn path. Attribution: the vocabulary originates in the panel judges ([panel:judge-1.md#5] item 5 namespace|data-dir; [panel:judge-2.md#5] item 5 advisory|mechanical) and was fixed by the owner tiebreak assumption (e) — cite both; team-execution-model adopts it.

D4. **Assumptions (a)–(l) and their attribution.** They ARE owner-authored: the complete tiebreak note is now at `docs/discovery/evidence/panel/owner-tiebreak.md` (restored; the earlier copy was truncated). architecture-options must enumerate (a)–(l) explicitly in a new §5.0 "Owner assumptions adopted" (verbatim from the note, each one line) and correct every pointer ("listed in §4", "quoted in full in §5") to "§5.0 / evidence/panel/owner-tiebreak.md". Everything not in the note is synthesis-authored and must be labeled so where it matters.

D5. **Answer paragraph.** The owner has already re-worded the adapter sentence identically in architecture-options §5, README and minimal-poc-plan §1 ("one HarnessAdapter per installed harness — five planned … claude-code and codex in the PoC-A slice … exactly one CoordinationSubstrate implementation") and fixed the §5 table cell "suppresses ClawTeam's implicit profile … prepare_command still runs but appends nothing when no --task is given". Fixers MUST NOT change the answer paragraph text further. If a fixer must change the §5 component TABLE (architecture-options only), the owner will re-copy it into minimal-poc-plan afterwards — list it under cross_doc_edits_needed.

D6. **M-row coverage of the answer (architecture-options).** Add §5.1: a table of all M-priority requirement IDs from the register (AD-01,02,04,05,07,08,09; TC-01,02,05,06; TE-01,02,03,04,05,07,08; HB-01,02,03,05; AR-01,02,03,04; EV-01,02,03,04; MS-01,04; XC-01,02,03) → component(s) of §5 → PoC/walkthrough that exercises it → status ∈ {full, partial, documented-only, deferred-after-PoC}. Reconcile AR-03: O2 "cannot satisfy (metadata-only)" vs §5 row 6 — state "metadata-only in the smallest layer (manifest/lock/report, no installer): partial by design; re-establishment on another host is a manual step". AR and EV components are "built, not falsified by any PoC" — say so.

D7. **Fit-gap "Minimum reuse rung" columns and XC rows.** Relabel every roll-up column "Minimum reuse rung needed (1-6)" to "Reuse seams observed (non-binding; rungs are decided in reuse-vs-build-analysis.md)" and add one sentence in Method stating it. XC-03 row: retitle "XC-03 — reuse-ladder reading for the AD/EV layers only (cells denote observed reuse rung R1–R6, not a fit class)" and rewrite its cells as `R1!`, `R2~` etc.; XC-01 row: rewrite cells as licence marks (`MIT!`, `MIT~`, `prop.w`, `none!`, `?`) and say so in its header. Re-score AD-07 vs TE-04 vs TC-04 column-by-column with ONE rule: (i) "hidden" is the TC-04 row's concern only — AD-07/TE-04 score the ability to create a temporary member at run time; (ii) "requires a running Gateway" lowers confidence, not class (OC keeps its class with `!` or `~` as evidenced), and the same fact yields the same class in every row; recompute Best/G-AD-07/G-TE-04. Re-score TE-05 CT/CTF to `Xs!` (glue code — parent link, result carrier, isolation record, stop-before-cleanup — is new code, not configuration; legend P requires no new code) and make G-TE-05 and the TE layer summary match; apply the same reading to TE-07 CT, HB-04, HB-05, TC-03 DC (if any of them rests on "our layer carries it", it is Xs, not P) — state the rule once in Method.

D8. **reuse-vs-build.** State the bolding rule unambiguously: "bold = the lowest rung at which the requirement (as worded in the register) is actually satisfied; when a NEW data object is required, the object is rung 6 and its materialization rung 1–2 — bold 6 and annotate '1–2 to materialize'". Re-bold the four deviating rows (G-AD-03, G-AD-05, G-EV-04, G-TC-03) under that rule; recompute the tally; for G-TE-08 bold rung 1 (`skip_permissions=false` in the layer-written ClawTeam config — adapters.py:49 guards os.getuid() at :53, config default config.py:59) + 2 (wrapper captures exit code), with 3 (PR #159) / 4 (platform_compat) optional, risk "high (no Windows CI; unprobed)"; for G-TC-04 add the rung-2 shadow (layer-side hidden flag + filtered roster over `team status --json`, the "two rosters" the architecture adopts); keep the sentence "rung 3 is never the only path" only if every rung-3 row shows its rung-2 twin. Hand the new tally to architecture-options in cross_doc_edits_needed (architecture-options must then cite "rung 6 only for the data objects listed in reuse-vs-build §6" without quoting a count unless it matches).

D9. **Ephemeral Assistant origin (assistant-domain-model §8).** Remove `origin{run,…}` from the Ephemeral Assistant *definition*; the definition stays run-fact-free so the §4 validator applies unchanged; origin/created_by/definition_ref live on the Member/roster entry (team-execution-model §5). §3 "Edited by" column vs §9 conflict table: make §9 the single source; delete the §3 column and point to §9.

D10. **TC-05 enforcement level (team-execution-model §5, architecture-options).** State the achievable level on ClawTeam today verbatim: "gate + convention + run-scoped `CLAWTEAM_BIN`/PATH shim that refuses raw `spawn` inside a run + post-hoc roster/registry reconciliation"; the Lead CAN bypass by calling `clawteam spawn` directly ([ev:clawteam-spawn-platform#F16] no pre-spawn hook on the CLI path). Mechanical enforcement needs the upstream hook PR or owning the spawn path.

D11. **ClawTeam adapter mechanism (team-execution-model §4).** State: the layer spawns `clawteam spawn subprocess -- <ats-wrap …>` with an UNRECOGNISED wrapper basename, passes NO `--task` (so `adapters.py:131-140` appends no prompt), writes `skip_permissions: false` in the layer-owned ClawTeam config (so `os.getuid()` at adapters.py:53 is never evaluated), and the wrapper delivers the injection bundle from a file; cite [ev:clawteam-spawn-platform#F1][#F5][#F10][ev:clawteam-probe-log#F12].

D12. **Broker location / scheduler pointers.** harness-broker-model §6 and §13 (Q7) and team-execution-model §13 (Q3) add one sentence: "Decided in architecture-options §5/§8: the HarnessBroker runs inside the layer as library/CLI (never as a ClawTeam backend; the in-process backend is the Windows fallback only); the operational-mode scheduler is an OS timer by default with OpenClaw/Hermes cron pluggable."

D13. **harness-broker-model HBM-01.** Reword §8: Claude/Codex/Grok enforce the verdict schema by flag (`--json-schema` / `--output-schema` / `-o`); Hermes (`-z`) and OpenClaw (`agent --json`) receive the schema in the prompt and are validated by the broker.

D14. **legacy-atm-disposition F-01 (BLOCKER).** Rewrite §1 item 4 exactly as the critic proposes: ATM's persistent objects (materialization records ADR 0016, ProjectRoleContext ADR 0017, Deployment plane ADR 0009/0018, A2A bus ADR 0023/0025/0026) are deployment objects; of architecture §2's four "non-negotiable distinctions" (RoleDefinition ≠ Runtime Agent; ProjectContext ≠ team-global conversation; Team semantics ≠ messaging-surface semantics; capability requirement ≠ runtime-specific implementation), three survive as K1/K11/K6 and only the second is demoted. Cite ATM/docs/design/2026-08-19-architecture.md:18-25. Fix V4 citation ([ev:harness-cli-capabilities-b#F15] + ATM fixture path) and close open question 6 by anchoring every parenthetical to real §n headings; cite every mapping cell or mark `?`.

D15. **General.** Keep frontmatter status "draft v2 (W3 findings applied 2026-08-22)". Do not add requirements. Do not touch another document. Record rejected findings with evidence. The answer paragraph is frozen (D5).

---

## Provenance note (2026-08-23, G1 documentation hygiene — review R18/R19, ADR 0018/0022)

These decisions were written on 2026-08-22 by the orchestrating session in the owner's voice, as binding inputs for the W3 fix pass. ADR 0018 reclassifies them (together with tiebreak assumptions (a)–(l)) as historical, session-authored inputs rather than human owner decisions; ADR 0022 records which of them still stand (D3 `independence {declared, achieved}` — now a glossary term), which were reversed later (D7 by the M0.1 AD-07 correction, F9), and which describe the superseded CLI-over-ClawTeam mechanics (D1, D10, D11). The text above is unchanged and remains the dated record of what the fix pass applied.
