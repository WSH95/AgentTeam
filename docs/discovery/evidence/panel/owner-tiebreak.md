---
id: panel:owner-tiebreak
date: 2026-08-22
role: session owner tiebreak note passed verbatim (as the `tiebreak` argument) to the W2b-2 synthesis agent
note: an earlier copy of this file was truncated (it reproduced only the first paragraph as quoted by the synthesis); this is the complete note. Assumptions (a)–(l) below are owner-authored; the synthesis agent authored everything else in architecture-options.md.
---

# Owner tiebreak note (verbatim, complete)

OWNER TIEBREAK (session owner, 2026-08-22). Both judges endorse the same named composition; adopt it as THE answer: 'A's O2 scope with B's two seams' — a thin, format-independent Assistant/Team layer that (1) owns as substrate-neutral data: Assistant definitions (closed schema + exclusion validator), TeamTemplates by reference, Base/User/Reviewed-Evolution overlays + Proposal/review record, HarnessProfiles as data + HarnessSelectionPolicy (user > Assistant > team > default), HarnessInvocation ledger + Ensemble synthesis record with per-invocation attribution, artifact manifest + lock + per-host resolution report, TeamRun record/roster projections (visible vs archive) + nesting contract (parent link, result carrier, isolation level recorded, stop-before-cleanup) + archive; (2) declares exactly two seams — HarnessAdapter {profile data, injection recipe, invoke, parse} and CoordinationSubstrate {create_space, add_member, create/update/wait task, send/receive, snapshot, stop, cleanup} — with ONE implementation each today: five harness adapters (claude-code, codex, grok, hermes, openclaw) and a ClawTeam adapter over its CLI (pinned 0.3.0@0119833, mcp<2, subprocess backend, no tmux), plus a trivial 'direct' launcher (no DAG) for solo runs and ensemble legs (PoC A); (3) builds NO second team substrate (Claude Code teams / OpenClaw sessions / Hermes kanban cannot host a mixed-harness nested TeamRun today), NO ClawTeam fork, NO library-seam coupling (C's in-process SpawnBackend is the documented Windows fallback only if Python is chosen and the CLI path fails the Windows probe), NO surface adapters (OpenClaw/Telegram stay O4, C-priority, outside the layer), NO artifact installer (metadata/lock/report only), NO UI. Ranking to state: O2+seams (composition) > O7 (B) > O5 filed-not-depended > O6 selective vendoring (platform_compat.py, model_resolution.py pattern, attribute HKUDS) > O3 (misnomer: the broker is part of the layer, not a ClawTeam backend) > O1 (fails M rows: EV-03, AD-05, TC-05, TE-05, HB-01/03) > O4 > O8 (not justified by evidence). Judge 1 Q9: prefer the smaller SCOPE with B's seams (this composition), not the smaller LOC claim.

ASSUMPTIONS the synthesis must adopt verbatim: (a) language-agnostic CLI coupling; implementation language is an owner question; (b) never pass --task through 'clawteam spawn' — the wrapper delivers the bundle from a file; skip_permissions=false in the layer-written ClawTeam config; (c) every ClawTeam spawn runs lifecycle on-exit on exit — one-shot legs use 'direct' and register nothing; (d) TC-05 is 'gate + convention + run-scoped CLAWTEAM_BIN shim + post-hoc roster/registry reconciliation' until enforced — label it so in R10; (e) TE-05 isolation level is RECORDED per nested run (independence.achieved: namespace | data-dir | mechanical), result carrier = task update --metadata AND inbox send, settled in PoC C; (f) two rosters accepted for PoC B (layer views vs ClawTeam board), stated as such; (g) read ClawTeam state via CLI --json/team snapshot; the only internal file read is spawn_registry.json (pids); (h) Hermes = first-class Harness and AR-03 package precedent, not a team substrate; (i) size by slice: PoC-A slice first (schemas + overlay resolver + claude-code & codex adapters + broker resolve/record + launcher/wrapper + ledger + ensemble record, ≈2–3k LOC), run layer gated on PoC A passing, operational mode + proposal generator after PoC C; reconcile A's 3–4k / B's 9–11k / C's 8k estimates against the slice plan, and present the total as 'M (≈4–6k LOC incl. data) with the PoC-A slice ≈2–3k'; (j) day-one probes are PoC preconditions: codex exec -c developer_instructions honoured + positional prompt without TTY; claude -p --bare --append-system-prompt-file under the wrapper; Hermes --source tool hiding/memory on 0.20.4 if a Hermes Member is used; one Windows + one macOS smoke (wrapper + one clawteam spawn subprocess); (k) file C's nine bounded PRs as intent (getuid guard, mcp<2, logs+exit code, created_by parent link, TeamMember.hidden, cleanup stops processes, Hermes/Grok branches + Codex/Grok system prompt, launch error surfacing + AgentDef.profile + TaskDef.blocked_by, inbox membership ACL) — plan as if none merges; (l) Codex cost attribution: synthesis record carries cost_source: unavailable|derived.

OWNER QUESTIONS to list in §7 (do not answer them): implementation language; Windows/macOS probe host + whether it is a precondition; acceptable enforcement levels for PoC pass (TC-05 gate+convention; TE-05 namespace/data-dir); file PRs now/later/never; unattended-run credentials (API keys vs subscription OAuth) for PoC A Run 3; Hermes Member in PoC B or Claude+Codex only; ATM licence statement for ADR 0022 schema text reuse; PoC budget + deterministic no-op-harness tier as first pass criterion; ensemble synthesis harness for Run 3; whether the Member coordination protocol may be ClawTeam-CLI-specific for the PoC or must be rendered per substrate from day one.

DISSENT to keep honest in §4: (i) B outright — seams are needed now because the only team substrate is dormant; (ii) C's in-process route is the only one where ClawTeam's registry/liveness/on-exit see the process and argv hazards vanish on Windows; (iii) O6 against ClawTeam entirely — vendor store/file.py + transport/file.py (~300 lines, MIT) and drop a dormant dependency with a broken MCP server; (iv) O1 first as a one-day zero-code demo to settle Codex-under-ClawTeam; (v) Hermes profiles + kanban as the cheapest substrate for single-harness operational (LO) teams. State what would change the answer (§6): ClawTeam upstream revives and merges the PR set; Hermes kanban gains nested boards + foreign-harness workers; Claude Code teams leave experimental and accept non-Claude members; Windows probe fails on the CLI path (→ C's in-process backend or O6 vendoring).
