# Risks

Known risks and mitigations. Review at wrap-up when something changed.

Columns added 2026-08-23 (G1 documentation hygiene, review H12): `ID` is stable and cited elsewhere as `RISKS R01` etc.; `Owner` names who or which gate carries the mitigation (`owner` = a human decision or approval, `M1a Gn` = a gate of the approved M1a plan, `steward` = project-process mitigation, a later milestone, or `accepted`); `Status` is `open` (mitigation planned or designed, evidence pending), `monitoring`, `mitigated`, `accepted`, or `closed`. All rows were last reviewed on 2026-08-23 at G1. W1-era mitigations in older rows are dated wording kept for traceability; the Status cell says what is current.

| ID | Risk | Likelihood | Impact | Mitigation | Owner | Status |
| --- | --- | --- | --- | --- | --- | --- |
| R01 | ClawTeam's alpha in-process seams, process-global event bus/config, and mutable data root can leak provider concerns or state across runs | high | high | Keep it an optional exact-revision extra; confine all imports to one compatibility module; clear/contain hooks and translate events; use opaque team namespaces; run a separate OS compatibility matrix; never use its `shell=True` subprocess backend or claim stronger isolation than demonstrated | M1a G4 (seam qualification) | open — mitigation designed in the M1a plan; evidence pending |
| R02 | ClawTeam-OpenClaw fork has structurally diverged (re-inlined per-CLI flags; 5 xfail tests) — selective reuse may be hard to keep merged | medium | medium | Classify fork features in W1; prefer upstream-friendly extension; reuse isolated modules (`platform_compat.py`, `model_resolution.py`) only with license + merge-cost notes | M1b+ (only if fork modules are reused) | monitoring |
| R03 | Telegram platform and OpenClaw support drift independently (managed bots, guest bots, Communities, ephemeral messages now exist at platform level but installed OpenClaw support is unverified) | high | medium | Record platform-vs-adapter capability separately; local installed-doc/dist recheck plus dated official sources; do not derive Team semantics from surface topology | later surfaces milestone | monitoring |
| R04 | Evidence volume overwhelms synthesis fidelity (drafters invent abstractions no substrate supports) | medium | high | Evidence schema with requirement IDs; domain models drafted after fit-gap rows exist with a mandatory "mapping to substrates" section; adversarial critics | steward (M0 method) | closed — M0 delivered; retained as a lesson for later multi-agent work |
| R05 | ATM sunk-cost leakage (demoted concepts re-enter as architecture) | medium | medium | Glossary blocklist; critics grep for demoted nouns; ATM used only via `atm-salvage.md` | steward (glossary/critics) | monitoring |
| R06 | Harness CLIs change flags between versions (Claude Code, Codex, OpenClaw move fast) | high | low–medium | Pin versions in every HarnessCapability table; record the date; treat capability tables as HarnessProfile data, not constants | M1a G3/G5 (profiles as data) | open |
| R07 | Workflow cost/time (≈36–45 agents) exceeds expectations | medium | low | Each workflow ≤10 agents; resume via run IDs; owner holds anchors | steward (M0 method) | closed — M0 discovery complete |
| R08 | ClawTeam upstream dormant (last commit 2026-05-09, 22 open PRs; `clawteam-mcp` broken by unpinned `mcp`) — bug fixes and PRs may never land | high | medium | Pin full commit `01198332ef9270c32c5460b8a178f964fc0df451` plus `mcp>=1,<2`; isolate imports behind the CoordinationSubstrate seam; keep bounded PRs optional and plan as if none merges; a provider failure must not affect the direct core | M1a G4 / M1b | open |
| R09 | Windows/macOS paths are not available on physical owner hosts; hosted CI can miss live auth/model/runtime behavior | high | high | GitHub-hosted deterministic direct-runner and optional ClawTeam compatibility jobs; no live credentials; label claims as OS/import/path/process plumbing only; retain Ubuntu owner-host live acceptance | M1a G2/G7 (hosted CI) | open |
| R10 | Harness CLI drift (weekly releases; Codex `developer_instructions` path unverified; Grok flags partly undocumented) | high | medium | HarnessProfiles as data with verification levels; per-adapter `--help` smoke test on each version bump; day-one probes listed in the PoC plan | M1a G5 (day-one probes) | open — overlaps the flag-change row above; kept for the probe obligations |
| R11 | Dynamic-member creation bypasses policy if code reaches a coordination provider outside the AgentTeam boundary (TC-05) | medium | high | M1c exposes only the product-owned policy-and-spawn path to AgentTeam members; keep provider imports private to one module; audit attempted/out-of-band mutations; scope the mechanical claim to AgentTeam-mediated creation | M1c | open |
| R12 | Model-facing tool-call non-compliance (duplicate hops, omitted calls, prose instead of a tool call) breaks autonomous choreography on some model/harness pairs — ATM U2 0/2 on DeepSeek/OpenClaw | medium | high | Reliability as a HarnessProfile dimension (HB-01); protocol state in the DAG and structured `atm` calls; deterministic fake-harness acceptance is mandatory before live evidence; archive semantic and mechanical outcomes separately | M1a G4/G6, M1b | open |
| R13 | Compliance vs mechanical failures conflated in evidence (ATM lesson) | medium | medium | HarnessInvocation record separates mechanical outcome (exit code, output) from semantic verification; PoC criteria name both | M1a G4/G6 (record design) | open — mitigation designed |
| R14 | An aborted run leaves no record and no postmortem (ATM exit-137) | medium | medium | Terminal status for every HarnessInvocation and TeamRun, written before the first side effect (XC-04, TE-07) | M1a G3 (process runner) | open — mitigation designed |
| R15 | LLM-dependent end-to-end evidence is flaky and costly (ATM R11) | high | medium | M1a separates deterministic evidence from live evidence, makes the deterministic tier a precondition, and caps one acceptance cycle at eight model calls with no automatic full-cycle repeat | M1a G4–G6 | open — mitigation designed (budget ceiling ADR 0020) |
| R16 | Subscription OAuth is misused as a hosted/multi-user credential service, or copied into CI | low | critical | Owner-operated persistent host only; AgentTeam never brokers login/copies credential stores; hosted CI credential-free; re-review terms before any multi-user/hosted product | owner | monitoring |
| R17 | API-test provider/model drifts or is mistaken for native harness evidence | high | medium | Separate `api-test` execution mode; profile stores base/protocol/model + credential env-name only; no native-auth fallback; selection stays unset until separate approval and verification, with claims absent until a canary runs | owner (route deferred; ADR 0017) | open — no route selected |
| R18 | PoC advisory controls are described as production security | medium | high | Every run records declared/achieved enforcement and bypasses; hidden is UI only; production acceptance requires mechanical enforcement | M1c+ (and every evidence record) | open |
| R19 | Documentation validation overclaims semantic review or owner approval | medium | medium | Keep original 54×11 structural lint distinct from targeted AD-07 semantic correction; record exact commands/results and never infer a future-dated/full-owner read-through | steward | monitoring |
| R20 | AgentTeam/`atm` names collide with similarly named public projects, Python distributions, or local commands | medium | medium | Use the namespace-qualified `WSH95/AgentTeam` repository; keep the distribution unpublished in M1a; recheck remote/PyPI/command availability immediately before rename or publication; state the product boundary clearly | M1a G2 (pre-first-push checks) | open — local `atm` none on PATH (G1); GitHub `WSH95/AgentTeam` and PyPI `agentteam` both 404 on 2026-08-23 (re-checked immediately before creation); distribution unpublished |
| R21 | Dedicated native-auth profiles expire or silently lose precedence to an API key/base URL/proxy inherited from the shell | medium | high | Owner logs in through each vendor CLI; `profile doctor` exposes sanitized status; native runs fail closed on API/base/provider conflicts and proxy-deny profiles; standard profiles inherit the explicitly owner-trusted terminal proxy with names-only evidence; never copy credentials or fall back to API mode | M1a G5 (`profile doctor`) | monitoring — all three native logins and current-version probes are ready; expiry/version drift still fail closed (ADR 0027/0030) |
| R22 | Raw model streams or workspace paths in local PoC evidence contain sensitive data | medium | high | Store full evidence only in a gitignored owner-only state directory; redact recorded argv/path/env metadata; never auto-add/export/upload; commit only human-reviewed sanitized summaries | M1a G6/G8 (redaction, evidence bundle) | open — mitigation designed |
| R23 | Claude headless subscription accounting changes again or stale docs imply a separate monthly Agent SDK credit | high | medium | Budget M1a by calls/time, not assumed USD/credit; follow the June 15 Help Center pause over stale overview text; recheck official policy before every live cycle | owner / M1a G6 | monitoring |
| R24 | Timeout/cancellation leaves vendor subprocess descendants alive, especially on Windows | medium | medium | One tested process runner; POSIX process-group termination and argv-based `taskkill.exe` tree termination on Windows; terminal records and post-run liveness checks | M1a G3/G5 (process runner) | open — revised runner passes local process/cancellation tests; new hosted Windows evidence pending push; 2026-08-24 review R4: the probe runner's kill-escalation has an unbounded branch (skip-SIGKILL when the direct child dies in grace + timeout-less final drain) — PLAN G5.R pre-G6 task (ADR 0032) |
| R25 | Python core reduces direct code reuse from TypeScript-native DSH/DG modules or makes a later DSH plugin less ergonomic | medium | low–medium | Reuse JSON/schema and mechanism ideas, not runtime modules; keep all external records language-neutral; add a small native TypeScript edge package only if DSH becomes a primary harness rather than porting the core pre-emptively | later (DSH edge decision) | accepted |
| R26 | An exact Git dependency makes optional-provider installs sensitive to repository availability and lock resolution | medium | medium | Commit `uv.lock`; verify fresh optional-extra installs in CI; record the full revision in package metadata and compatibility reports; never make the extra part of core installation | M1a G2/G4 | open — mitigation designed |
| R27 | Grok Build is a required first-pass harness while its active auth and definition-injection channel are unverified (M0.1 F2; `evidence/harness-cli-capabilities-a.md` §5) | medium | medium | Day-one probes recorded into the HarnessProfile before any PoC A cycle; FAIL-HARD report if the channel or auth fails; owner revisits the "all three required" gate on evidence (ADR 0018) | M1a G5 (probes) / owner | mitigated — final Grok 1.0.5 reassessment verified native auth, instructions, both Skill paths, schema, and field output; G5 closed (ADR 0031) |
| R28 | "Owner" decisions in M0 artefacts were session-authored in the owner's voice (tiebreak note, fix-pass D1–D15, assumptions (a)–(l)); later readers may treat them as human decisions | medium | medium | ADR 0018 reclassifies (a)–(l) as historical panel inputs; new decisions cite the human approval event; plans restate only what they need | steward | mitigated — ADR 0018 reclassified; ADR 0022 added amendment markers; monitoring |
| R29 | Steward records drift from the commit they ship in (HANDOFF at `3407ec9` described a pre-commit world; PLAN claimed critic re-checks with no artefact) | medium | low–medium | Rewrite HANDOFF inside the same commit as the work it describes; every "PASS" claim names its artefact; review hygiene H7–H9 tracked in the next plan's T0 | steward | mitigated — G1 hygiene commit applied H3/H6/H8–H12; monitoring |
| R30 | Windows npm `.cmd` shims run through `cmd.exe` even with `shell=False`; CI fakes (`python -m …`) never exercise that path, so hosted CI cannot prove vendor-shim behaviour | medium | medium | File/stdin delivery for all user-controlled content; state the TE-08 claim boundary (plumbing, not shim behaviour) in M1a §9; first Windows vendor-shim evidence comes from an owner-run smoke, recorded as such | M1a G3/G7 (launcher policy, vendor-smoke) | open — mitigation designed (M1a r3 §9) |
| R31 | The review/synthesis JSON Schemas may hit a feature one vendor's structured-output dialect rejects (strict mode: required-all, `additionalProperties:false`, nullable optionals) | medium | medium | Author schemas in the dialect intersection (M1a §7); G5 probes confirm acceptance per vendor before any acceptance cycle; schema changes are versioned | M1a G5 (probes) | mitigated — current Claude/Codex/Grok probes accepted the strict probe schema; monitor schema/version drift and retain G6 review-schema evidence |
| R32 | Claude Skills load only when safe mode is off; isolation now rests on a fresh `CLAUDE_CONFIG_DIR` + `--setting-sources user` + strict MCP rather than one flag | medium | medium | Explicitly pre-approve the built-in `Skill` loader under `dontAsk` while denying write/shell/web tools; G5 probe proves the channel and records it; hard timeout/tree-kill bounds upstream headless/deferred-tool stalls; fallback channels ordered in M1a r3 §11 | M1a G5 (probes) | mitigated — after the bounded timeout/fix, Claude's second/final call verified config-home Skill delivery and all five rows (ADR 0028/0029); 2026-08-24 review R3: the run-time managed-skills lease can leak on early-return/unexpected-exception paths, leaving managed Skills in the persistent home — PLAN G5.R pre-G6 task (ADR 0032) |
| R33 | Grok may discover `.grok/skills/` and `.agents/skills/` yet fail to load their bodies, or may change path/output spellings between versions | medium | high | Exact random body markers (never name-derived placeholders); `.grok/skills/` primary and `.agents/skills/` fallback recorded separately; explicit documented slash-name references; accept observed camel/snake structured fields and JSON-in-`text`; re-run on every Grok version bump | M1a G5 (probes) | monitoring — two corrected 1.0.5 calls reproduced both exact body markers through field output, including the final all-three reassessment; the earlier failure is retained and version drift still forces reassessment (ADR 0029–0031) |

Further ATM-derived candidate rows (R3, R5–R10) are listed in `docs/discovery/legacy-atm-disposition.md` §4; merge on demand.
